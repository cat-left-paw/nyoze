# Nyoze — 操作マニュアル

> 随時更新。機能追加時にセクションを追記する。

---

## 目次

1. [概要](#概要)
2. [編集モード一覧](#編集モード一覧)
3. [通常編集（WYSIWYGモード）](#通常編集wysiwygモード)
   - [付箋（Notes）](#付箋notes)
   - [作品（Project）タブ](#作品projectタブ)
   - [アウトライン（Outline）タブ](#アウトラインoutlineタブ)
   - [ルビ・傍点ダイアログ（統合UI）](#ルビ傍点ダイアログ統合ui)
4. [Paragraph Plain モード（段落単位ソース編集）](#paragraph-plain-モード段落単位ソース編集)
5. [Source Mode（全文ソース編集）](#source-mode全文ソース編集)
6. [マウスホイール・スクロール](#マウスホイールスクロール)
7. [表示設定](#表示設定)
8. [フロントマター表示](#フロントマター表示)
9. [ファイル操作](#ファイル操作)
10. [beta 版の既知制限](#beta-版の既知制限)

---

## 概要

Nyoze は縦書き（`writing-mode: vertical-rl`）/ 横書き（`horizontal-tb`）対応の Markdown エディタです。
ProseMirror / TipTap をベースとした WYSIWYG 表示と、Markdown ソース直接編集の両方を提供します。
起動時のデフォルトは縦書きです。上部ヘッダーのボタンでいつでも切り替えられます。

画面上部のヘッダーにはアプリ名、ツールバーボタン、ペイン切替、ウィンドウ操作が1行で配置されています。macOS ではネイティブメニューバーが、Windows/Linux ではハンバーガーメニューが追加のナビゲーション手段として利用できます。

---

## 編集モード一覧

| モード          | ボタン            | 説明                                                              |
| --------------- | ----------------- | ----------------------------------------------------------------- |
| 通常編集        | ―                 | デフォルト。装飾付き WYSIWYG 表示で編集する                       |
| Paragraph Plain | `Paragraph Plain` | フォーカス中のブロックのみ Markdown ソースに切り替えて編集する    |
| Source Mode     | `Source Mode`     | 文書全体を Markdown ソースとして横書きの source editor で編集する |

Paragraph Plain と Source Mode は同時に有効にできません。

---

## 通常編集（WYSIWYGモード）

### ツールバーボタン

| ボタン          | 機能                                                                                                                     |
| --------------- | ------------------------------------------------------------------------------------------------------------------------ |
| Undo / Redo     | 直前操作の取り消し / やり直し                                                                                            |
| Bold            | 選択範囲を太字にする（`**text**`）                                                                                       |
| Italic          | 選択範囲を斜体にする（`*text*`）                                                                                         |
| Strike          | 選択範囲に取り消し線を付ける（`~~text~~`）                                                                               |
| Highlight       | 選択範囲をハイライトする（`==text==`）                                                                                   |
| Underline       | 選択範囲に下線を付ける（`\|\|text\|\|`）                                                                                 |
| Inline Code     | 選択範囲をインラインコード化する（`` `code` ``）                                                                         |
| Clear Format    | 選択範囲の全インライン装飾を解除する。TCY を選択中なら通常テキストへ戻す                                                 |
| Horizontal Rule | 区切り線（`---`）を挿入する                                                                                              |
| Heading         | H1〜H6 への変更、見出し解除                                                                                              |
| Bullet List     | 箇条書きリストをトグルする                                                                                               |
| Ordered List    | 番号付きリストをトグルする                                                                                               |
| Checklist       | チェックリストをトグルする                                                                                               |
| Blockquote      | 引用ブロックをトグルする                                                                                                 |
| Code Block      | コードブロックをトグルする                                                                                               |
| Link            | 選択範囲または現在のリンクの URL を設定・解除する（`Cmd/Ctrl + Click` で外部ページを開けます）                            |
| Note            | キャレット位置へ付箋を追加する。付箋は右ペインの **Notes / 付箋** タブで確認・編集できる                                |
| Ruby            | 選択範囲にルビまたは傍点を付ける（統合ダイアログ。詳細は下記）                                                           |
| TCY             | 選択範囲を縦中横にする（`｟text｠`）。解除は TCY ボタンまたは Clear Format を使う                                        |
| Ruby View       | ルビ表示の ON/OFF を切り替える                                                                                           |
| 縦/横切替       | 縦書き（`vertical-rl`）⇄ 横書き（`horizontal-tb`）を切り替える。設定はローカルストレージに保存され、再起動時に復元される |
| View Settings   | フォント・行間・余白・テーマなどを設定するダイアログを開く                                                               |
| ファイルを開く  | 単独ファイル（`.md` / `.markdown` / `.txt`）を開く（`Shift` で新規文書）。フォルダを書庫として使う場合は File メニューの「書庫を管理」から登録する |
| Save            | 上書き保存（保存先未確定なら Save As にフォールバック、`Shift + Save` は常に Save As）                                   |

> 装飾ボタン（Bold / Italic / Strike / Highlight / Underline / Inline Code）は、カーソル位置または選択範囲の状態に応じて選択状態（枠線+背景）を表示します。
> 複数装飾が重なっている場合は、該当する複数ボタンが同時に選択表示されます。
> `小説・本文` / `未設定` のような Nyoze 既定の改行ポリシーでは、複数の段落や見出しを選択して **Blockquote** / **Code Block** を適用すると、本文向けに自然な 1 つの引用 / コードブロックへまとめます。再度トグルして解除すると、引用内の改行やコードブロック内の `\n` を通常の段落へ戻します。リストや既存の複雑なブロックをまたぐ選択、付箋アンカーを含む範囲では安全側で通常の挙動に戻します。
> 右ペインの **文書メタデータ**（英語 UI では **Document Metadata**）タブでは、現在の Markdown ファイルの **Document Type**（`小説・本文` / `記事・文書` / `未設定`、英語 UI では `Fiction` / `Article / Document` / `Unset`）や `title` / `author` / `translator` などを編集できます。

### コードブロック表示

- WYSIWYG のコードブロックは、fenced code の言語名が対応範囲に入る場合だけ表示専用のシンタックスハイライトを行います。
- `python` / `javascript` / `typescript` / `json` / `css` / `bash` / `markdown` などの common language と、`py` / `js` / `ts` / `html` / `sh` などの代表的な alias に対応します。
- 言語未指定または未対応言語のコードブロックは自動判定せず、プレーン表示になります。
- コードブロック上部には言語ラベルと `Copy` ボタンが表示されます。`Copy` はコード本文だけをコピーし、fence 記法や言語名は含めません。
- シンタックス色は文書テーマの実効色に合わせて自動調整され、カスタム文書テーマにも反映されます。
- これらは表示のみの機能です。Markdown、frontmatter、保存形式、`Source Mode` の内容は変更しません。

### リンク

- `Link` ボタンまたは `Cmd/Ctrl + K` で、選択範囲や現在位置のリンク URL を設定・解除できます。
- 通常編集上のリンクは、`Cmd/Ctrl + Click` のときだけ外部ブラウザで開けます。通常クリックでは開きません。
- 開けるリンクは `https://` の URL のみです。`http://`、`mailto:`、`tel:`、`file:`、`javascript:`、`data:`、相対リンク、文書内アンカーは開きません。
- Source Mode ではこのリンクオープン導線は使いません。

### 独自ブロック装飾（中央寄せ・字下げ・スタイル）

Nyoze には、段落のまとまりを装飾するための独自記法があります。`:::` で囲んだ
fenced directive block として書きます。

```
:::align-center
第一章
:::

:::indent-3
字下げする本文
:::

:::style-letter
拝啓
:::
```

- 対応する directive
  - `align-center`: 中央寄せ
  - `align-end`: 行末寄せ（横書きでは右寄せ、縦書きでは地付き相当）
  - `indent-1`〜`indent-6`: 段階的な字下げ（writing-mode に追従）
  - `style-<id>`: 意味を表す semantic style block
- `style-<id>` の `<id>` は保存上の semantic id です。組み込みの `style-letter`（手紙風）/ `style-muted`（控えめ）/ `style-heading`（章題風）に加え、`style-scene-note` のような未知の id も消えずに保持されます。
- 表示は控えめで、文書テーマの色に合わせます。文字色やフォントの直接値は本文 Markdown には保存しません。
- 無効な記法（例: `:::style-` / `:::style-手紙` のような日本語混じり id / `:::indent-7`）は装飾化せず、通常テキストとして保持します。
- ツールバーの「ブロック装飾」メニュー（段落アイコン）から、現在のブロックまたは選択範囲の段落へ装飾を適用できます。
  - 適用できるのは「中央揃え」「右寄せ / 地付き」「字下げ 1〜6」「スタイル: 手紙 / 控えめ / 見出し」です。
  - すでに装飾が付いているブロックで別の項目を選ぶと、入れ子にせず現在の装飾を置き換えます。
  - 「装飾を解除」で囲みを外し、中身の段落だけを残します。
  - Undo / Redo は通常の編集と同じように使えます。
  - `Source Mode`・`Paragraph Plain`・読み取り専用ドキュメント・日本語入力（変換中）では無効になります。
- 未知の `style-<id>` の手入力 UI、ユーザー定義スタイル、作品ごとの style 定義はまだありません。記法は `Source Mode` で直接入力することもできます。

### 改ページ marker（`:::page-break`）

`:::page-break` は、本文中の明示的な改ページ位置を表す独自記法です。上記の中央寄せ・字下げ・スタイルとは別の、本文テキストを持たない専用 marker です。

```
:::page-break
:::
```

- 通常表示（WYSIWYG）では、本文操作を妨げない表示専用の「改ページ」marker として表示されます。文字を入力したり中に文章を入れたりすることはできません。
- 保存 Markdown は常に上記の 2 行（`:::page-break` と `:::`）の形へ戻ります。間に本文を書いた `:::page-break\n本文\n:::` のような形は改ページとして解釈されず、安全側で通常テキストとして保持されます。
- ツールバーまたは右クリックメニューの **ブロック装飾** メニューから改ページを挿入できます。改ページ marker を選択しているときは、同じメニューから削除できます。`Source Mode` で上記の記法を直接入力することもできます。
- **LeME 互換 / でんでんコンバーター向け Markdown / 青空文庫風 TXT の active document 書き出し**では、`:::page-break` を各形式の改ページ表現へ変換します。保存ダイアログの前に開く書き出しオプション確認 UI で、改ページの反映有無・見出し前自動改ページ・対象見出しレベルを選べます（詳細は各書き出し節を参照）。
- **Book 全体の書き出し**（File メニュー）でも同じ options 確認 UI を使い、加えて章境界の改ページ option を選べます（[Book 全体の書き出し](#book-全体の書き出し)）。
- 外部書き出しと Page Viewer では、`:::page-break` は「空白ページを作るブロック」ではなく「次の有効な本文ブロックを新しいページから開始する制御命令」として扱います。改ページの前後にある空段落は、書き出し / 表示用の変換では余計な空白ページを作らないよう調整されます。意図的な空白ページは別記法の `:::blank-page` を使います（次項）。
- 書き出しオプション確認 UI の選択内容の永続化、Vivliostyle / EPUB / PDF への直接出力は未対応です。

### 空白ページ marker（`:::blank-page`）

`:::blank-page` / `:::blank-page-N` は、意図的な空白ページを表す独自記法です。`:::page-break`（次の本文を新しいページから開始する制御命令）とは異なり、その位置に実際に N 枚の空白ページを挿入する固定ページ要素です。

```
:::blank-page
:::
```

```
:::blank-page-4
:::
```

- `:::blank-page` は `:::blank-page-1` と同じ意味（count 1）です。`N` は `1`〜`20` まで指定できます。
- 通常表示（WYSIWYG）では、count が 1 のとき「空白ページ」、2 以上のとき「空白ページ x N」と表示される、本文操作を妨げない表示専用 marker です。選択して `Delete` / `Backspace` で削除できます。
- 保存 Markdown は count 1 なら常に `:::blank-page` へ、2 以上なら `:::blank-page-N` へ正規化されます（`:::blank-page-1` と書いても保存時は `:::blank-page` になります）。
- `blank-page4`（ハイフンなし）・`blank-page-0`・`blank-page-999`（範囲外）・`blank-page-many`（非数値）や、間に本文を書いた `:::blank-page\n本文\n:::` のような形は空白ページとして解釈されず、安全側で通常テキストとして保持されます（自動的な丸め補正は行いません）。
- ツールバーの **ブロック装飾** メニュー、または右クリックメニューの **ブロック装飾** サブメニューの「空白ページを挿入」から挿入できます。枚数は `1枚` / `x2` / `x3` / `x4` / `x5` / `x10` / `x20` のプリセットから選べます（それぞれ `:::blank-page` 〜 `:::blank-page-20` を挿入）。`Source Mode` で上記の記法を直接入力することもできます。任意の枚数を自由入力する UI、専用の削除 UI はありません（削除は marker を選択して `Delete` / `Backspace` を使います）。
- `:::blank-page` 同士の間にある空段落は、`:::page-break` 前後の空段落とは違い、書き出し時に自動削除されません。たとえば `:::blank-page` と `:::blank-page-5` の間に空行を入れると、LeME / でんでんなどの変換結果では、その空段落が追加の空白ページのように見える場合があります。空白ページを合計 6 枚だけ入れたい場合は、2 つの marker の間に空行を挟まないか、`:::blank-page-6` を使ってください。
- **LeME 互換 / でんでんコンバーター向け Markdown の書き出し**では、`:::blank-page` を count 個の「前後両方に改ページ指定を持つ空 HTML ブロック」として近似出力します。実機確認では LeME / でんでん経由の EPUB で空白ページとして表示されることを確認しています。**青空文庫風 TXT の書き出し**では、標準の改ページ注記 `［＃改ページ］` の組み合わせ（間に最小限の空白 filler を挟む）で近似します。AozoraEpub3 変換での正確な見え方は未確認です。書き出し自体は失敗しないものの、他の近似・省略と同様に「一部の装飾は近似または省略されました」という通知が表示されます（個別の警告内容を一覧表示する UI は今回のバージョンにはありません）。既存の `:::page-break` の出力形式には影響しません。`pageBreak: false`（改ページを反映しない設定）を選んでいても、`:::blank-page` はユーザーが明示した固定ページとして扱い、この設定に関係なく出力されます。

### 右クリックメニュー（コンテキストメニュー）

通常編集モード（WYSIWYG）でエディタ領域を右クリックすると、以下の操作メニューが表示されます。

| 項目                  | 機能                          | disabled 条件                   |
| --------------------- | ----------------------------- | ------------------------------- |
| Undo                  | 直前操作の取り消し            | 実行不可時（履歴なし or IME中） |
| Redo                  | やり直し                      | 実行不可時（履歴なし or IME中） |
| Cut                   | 切り取り                      | 選択なし、IME中など             |
| Copy                  | コピー                        | 選択なし、IME中など             |
| Paste                 | 貼り付け                      | IME中など                       |
| Paste as Plain Text   | `text/plain` のみで貼り付け（HTML を無視） | Paste と同じ（IME中など）       |
| Add Note              | キャレット位置へ付箋を追加    | project未所属、内部文書、IME中など |
| Select All            | 全選択                        | 文書空、IME中など               |
| Bold                  | 選択範囲を太字にトグル        | 選択なし or IME中               |
| Italic                | 選択範囲を斜体にトグル        | 選択なし or IME中               |
| Strikethrough         | 選択範囲に取り消し線をトグル  | 選択なし or IME中               |
| Highlight             | 選択範囲にハイライトをトグル  | 選択なし or IME中               |
| Underline             | 選択範囲に下線をトグル        | 選択なし or IME中               |
| Heading               | H1〜H6、見出し解除            | IME中                           |
| Block Decoration      | 配置 / 字下げ / スタイル / 改ページの操作 | IME中、Source Mode / Paragraph Plain 中など |
| Bullet List           | 箇条書きリストをトグル        | IME中                           |
| Ordered List          | 番号付きリストをトグル        | IME中                           |
| Checklist             | チェックリストをトグル        | IME中                           |
| Move List Item Up     | リスト項目を一つ上に移動      | リスト外 or 先頭項目            |
| Move List Item Down   | リスト項目を一つ下に移動      | リスト外 or 末尾項目            |
| Insert Ruby           | ルビ / 傍点ダイアログを開く   | 選択なし or IME中               |
| TCY                   | 選択範囲を縦中横にする        | 選択なし or IME中               |
| Clear Format          | 選択範囲の全マークを解除      | 選択なし or IME中               |

**閉じ方**: Escape / メニュー外クリック / スクロール / リサイズ

- **Paste**: クリップボードに意味のある rich HTML があるときはそのまま貼り付け、ブラウザ等が付与するだけの薄い HTML ラッパーのみのときは、プレーン文字列を Markdown として解釈する経路に寄せます（`Article` / `commonmark-strict` では単改行が段落分割されにくくなります）。
- **Paste as Plain Text**: 常に `text/plain` だけを使います（`text/html` は無視）。Markdown 記法（例: `**太字**`）は従来どおり解釈されます。コードブロック内では Markdown 経路に入らないため、同じプレーンテキストを直接挿入します。クリップボードの読み取りに失敗した場合はインライン通知が表示されます。

> Paragraph Plain / Source Mode 中は右クリックメニューは表示されません。

### 付箋（Notes）

付箋は、本文中の位置へメモを紐づけるための機能です。本文には位置保持用の短いHTMLコメントアンカーだけを保存し、付箋のタイトル・本文・状態は project root の `.nyoze/notes.json` に保存します。通常の本文、コピー、将来の出力には付箋本文を混入させません。

#### 使う前の条件

付箋は Nyoze project に属するMarkdownファイルでだけ使えます。対象ファイルの祖先フォルダに `.nyoze/project.json` が存在している必要があります。作品未所属のときは、右ペインの **作品** タブから現在ファイルの親フォルダを作品化できます（詳細は [作品（Project）タブ](#作品projectタブ)）。

projectに属さないファイルでも通常編集はできますが、付箋追加は無効になります。単独ファイルの親フォルダをNyozeが勝手にproject rootとして扱ったり、`.nyoze` フォルダを自動作成したりすることはありません。

#### 追加

1. WYSIWYG通常編集で、付箋を置きたい位置へキャレットを置きます。
2. ツールバーの **Note / 付箋** ボタン、または右クリックメニューの **付箋を追加** を選びます。
3. 初回だけ、本文Markdownへ位置保持用コメントが追加される説明が表示されます。
4. タイトル（任意）と複数行のメモ本文を入力します。

タイトルと本文がどちらも空の場合は保存できません。本文にはMarkdownを書けます。見出し、強調、斜体、リスト、inline code などは右ペインのpreviewで表示されます。raw HTMLは実行されず、文字として扱われます。

#### 表示と確認

- 本文中には小さな付箋markerが表示されます。
- markerにマウスを乗せると、短いpreviewが表示されます。
- markerをクリックすると、右ペインの **Notes / 付箋** タブが開き、対応するカードが短時間ハイライトされます。
- 右ペインの付箋カードはクリックで選択できます。
- **本文へ** を押すと、本文中の対応markerへ移動します。
- 長い付箋本文はデフォルトで途中まで表示され、**全文表示** / **一部表示** で切り替えられます。全文表示中も preview 領域には最大高さがあり、それを超える本文はカード内で縦スクロールできます（カード全体が縦に伸びすぎないようにするため）。
- 各付箋カードはタイトル行の折りたたみボタンで、本文 preview を隠してタイトル・タグ・操作ボタンだけ残せます。折りたたみ状態は画面表示だけで、再起動や文書切替では保持されません。
- **編集** からタイトル・本文に加え、付箋の**色**（黄・灰・青・緑・桃・紫）を選べます。色は `.nyoze/notes.json` に保存され、右ペインのカードと本文中の marker に反映されます（Markdown 本文には書き込まれません）。本文中の marker にマウスを載せたときの簡易 preview も、付箋色を薄く反映します。hover preview は短文の確認用であり、全文の確認は右ペイン **Notes / 付箋** タブで行ってください。
- **色**は付箋ごとの見分け用です。**解決済み**は `status` で管理し、一覧のセクション分けに使います。**タグ**は作品内で共有する分類ラベルで、右ペイン上部の **付箋タグ** エリアで最大6件まで登録します。登録済みタグだけが一覧表示され、**タグを追加** から新規登録、各行の編集・削除から名前変更や削除ができます。追加・編集時は候補（要確認 / 加筆など）を選ぶか、自由入力できます。使用中のタグを削除しても付箋自体は削除されず、そのタグだけが付箋から外れます。タグ名と付箋への割り当ては `.nyoze/notes.json` にだけ保存され、Markdown 本文 / frontmatter には書き込まれません。タグに色は付きません。
- **表示フィルタ**で、登録済みタグごとに当該文書の付箋一覧を絞り込めます（**すべて** で解除）。フィルタは画面表示だけを変え、`.nyoze/notes.json` や Markdown 本文は変更しません。
- 本文中の付箋マーカーをまたいでテキストを選択した状態で右クリックしても、通常の編集メニューが優先されます（付箋専用メニューはマーカー単体を右クリックしたときだけ）。

#### 編集・状態変更・削除

右ペインの **Notes / 付箋** タブでは、現在の文書に紐づく付箋を確認できます。

- **編集**: タイトルと本文を編集します。本文Markdown自体は変更しません。登録済みの付箋タグを複数付与できます。
- **解決済みにする**: 付箋を resolved 状態へ移動します。
- **未解決に戻す**: resolved の付箋を open 状態へ戻します。
- **付箋を削除**: 本文markerを削除し、`.nyoze/notes.json` 側は `deleted` 状態へ変更します。

削除済み付箋は通常一覧には表示されません。現時点では、削除済み付箋の復元UIや完全削除UIは未実装です。

#### 不整合の整理

外部編集や過去の不具合により、本文markerと `.nyoze/notes.json` がずれることがあります。Nyozeは自動削除せず、手動で整理できる導線を出します。

- 本文markerがない付箋は、右ペインの **本文中のマーカーが見つからない付箋** セクションに表示されます。削除すると `deleted` 状態になります。
- 参照先ファイルが見つからない付箋は、**参照先ファイルがない付箋** セクションに表示されます。個別または一括で `deleted` 状態にできます。
- 本文にmarkerだけが残っていて対応するopen noteがない場合は、marker上の右クリックメニューから **マーカーのみ削除** できます。この操作では `.nyoze/notes.json` は変更しません。

NyozeのFile Explorer内で同じ作品内の単一ファイルをリネームまたは移動した場合は、付箋データの `file` も追従します。ファイルを削除した場合は、付箋を自動削除せず、missing-file として整理できるようにします。

作品に登録済みの本文 / 資料ファイルを、同じ作品の中で File Explorer からリネーム / 移動したときは、物理ファイルだけでなく `.nyoze/books.json`（作品登録）の path と `.nyoze/notes.json` の `file` がまとめて更新されます。作品登録 (`books.json`) が壊れている / 移動先 path が作品内で衝突する / 別の作品へ動かそうとする場合などは、安全のためファイルを動かさず通知欄に理由を表示します。更新の途中で作品登録 / 付箋データの保存に失敗したときは、ファイルを元の場所へ戻します。

File Explorer は、Nyoze の作業対象を一覧表示し、軽い単一ファイル操作を補助するためのものです。Finder / Explorer の代替になる本格的なファイルマネージャではありません。作品をまたぐ移動、フォルダごとの整理、複数項目の一括移動、ドラッグ＆ドロップなどの複雑な整理は OS 標準の Finder / Explorer で行ってください。外部で移動したファイルは、元の作品では missing、移動先の作品では未登録ファイルとして扱われます。

次の場合は安全のため操作できません。

- 編集中（未保存）のファイル、または Source Mode / Paragraph Plain で入力途中のファイルは、保存・確定するまでリネーム / 移動できません。
- 作品の中で、既存ファイルへ上書きする形でのファイル移動はできません（別名で移動してください）。移動後に作品登録 / 付箋データの追従が走るため、上書きは安全に元へ戻せない場合があるためです。
- 登録済みの本文 / 資料、または付箋を含むフォルダの名前変更は現在対応していません（フォルダ配下の一括追従が未対応のため）。フォルダごとの移動や複数ファイルの一括移動、ドラッグ＆ドロップも現時点では対象外です。

Mac と Windows の間では、日本語ファイル名の濁点 / 半濁点などが Unicode の別表現で保存されることがあります。Nyoze は `notes.json` の保存文字列を勝手に書き換えず、表示・検索・missing-file 判定の比較時にこの差分を吸収します。

#### Source Modeでの扱い

Source Modeでは、本文中の付箋アンカーが次のようなHTMLコメントとして見えます。

```markdown
<!-- nyoze-note:ID -->
```

このコメントを削除すると、本文中の付箋位置は失われます。Obsidianなど外部エディタで編集する場合も、このコメントを削除しないよう注意してください。WYSIWYG通常コピーでは、このコメントは外部clipboardへ含めません。

### 作品（Project）タブ

> UI では「作品」と表示します。内部仕様やファイル名では Project と表記する場合があります。

右ペインの **作品** タブは、作品全体を意味的に見渡すための索引です。左ペイン File Explorer の物理フォルダ階層のコピーではありません。

#### 用語

| 用語 | 意味 |
| ---- | ---- |
| **書庫（Workspace）** | Nyoze で通常時に開いて使う基本フォルダ。複数の作品を含めてよい |
| **作品（Project）** | `.nyoze/project.json` を持つフォルダ。付箋、Book 一覧、資料分類の単位 |
| **Book** | 作品内でまとめて閲覧・出力する単位。本編、外伝、短編集など |

project root は、現在ファイルから祖先フォルダを上へ辿り、最も近い `.nyoze/project.json` を持つフォルダとして解決されます。workspace root 直下を自動で project 扱いすることはありません。

書庫は File メニューの **書庫を管理** から作成 / 既存フォルダ登録 / 切り替え / 名前変更 / 登録解除 / Finder・Explorer 表示を行います。同時に active な書庫は 1 つだけです。書庫を登録または作成しても、そのフォルダに `.nyoze` は自動作成されません。`.nyoze/project.json` と `.nyoze/books.json` は、作品を作成したときに project root 側へ作られます。

#### File Explorer / Notes タブとの違い

| 場所 | 役割 |
| ---- | ---- |
| **左ペイン File Explorer** | 物理ファイル構造の一覧表示、単一ファイルの作成 / 名前変更 / 移動 / 削除などの軽い整理 |
| **作品タブ** | Book 一覧、Materials 一覧、未登録ファイルの登録、資料 preview / 簡易編集、作品作成 / 切り替え |
| **Notes / 付箋タブ** | 本文中の位置へ紐づく付箋の管理（`.nyoze/notes.json`） |

付箋の復元 / 完全削除、duplicate anchor 修復などは Notes タブ側の将来候補であり、作品タブの機能ではありません。

左ペイン File Explorer では、`.nyoze/project.json` を持つフォルダ（= 作品 / project root）の行に、作品タブと同じ小さな作品アイコンのバッジが付きます。バッジは表示専用で、フォルダの開閉やファイル操作には影響しません。書庫（workspace）そのものを自動的に作品扱いすることはなく、`.nyoze/project.json` を持つフォルダだけが対象です。

File Explorer の左ペイン上部には **書庫** / **作品** のタブがあります。**書庫** は active 書庫 root の通常ファイルツリー、**作品** は書庫内の作品一覧と、選択した project root の drill-down 表示です。作品一覧で作品を選ぶと、その project root のファイルツリーへ切り替わり、上部の **一覧** breadcrumb で作品一覧へ戻れます。**書庫** タブを押すと active 書庫 root の通常ファイルツリーへ戻ります。File Explorer で行を選択するだけでは、右ペインの 作品タブの表示対象は切り替わりません。

書庫外の単独ファイルをツールバーの **ファイルを開く** から開いた場合、左ペイン上部に「書庫外のファイル」として表示されます。この状態でも通常編集はできますが、Project / Book / Notes など書庫・作品に依存する機能は、祖先 Project を安全に解決できる場合を除いて無効または未所属扱いになります。

作品タブは、基本的に中央でアクティブなタブの文書に連動します。別の作品を一時的に見たい場合は 作品タブ内の **作品を切り替える** を使えますが、アクティブなタブ / ファイルを切り替えると、再び現在の文書の作品へ連動します。

#### 作品未所属のとき

アクティブな文書がどの作品にも属していない場合、作品タブには **このフォルダを作品として設定** が表示されます。対象フォルダ名は UI 上に明示されます。実行すると作成フォームが開き、**作品名** と **最初の Book 名** を確認してから作成します。

左ペイン File Explorer のフォルダを右クリックして **このフォルダを作品にする** を選んでも同じフォームが開きます。作成時には `.nyoze/project.json` と `.nyoze/books.json` が作られ、`books.json` には最初の Book が 1 件登録されます。すでに作品になっているフォルダ、既存作品の内側、既存作品を子孫に含むフォルダ、書庫 root 自体ではこの項目は無効または main 側で拒否されます。作成に失敗した場合は File Explorer の通知欄に理由が表示されます。

- 書庫（workspace）外の単独ファイルでは作品化できません
- すでに `.nyoze/project.json` があるフォルダは上書きしません
- 既存作品の内側や、既存作品を含む親フォルダは作品化できません
- 書庫 root は作品化できません
- いずれの導線でも `notes.json`・本文/資料フォルダ・章テンプレートは作りません
- 本文フォルダ、資料フォルダ、章テンプレートは自動生成しません

#### Books と資料

作品内の Book 構造、資料分類、**表示 metadata**（Book 名 / 著者、本文・資料の title / authors / translators）の正本は、各 Markdown の frontmatter ではなく `.nyoze/books.json` **v3** です。作品タブの Book 作成、Book 名変更、本文・資料 metadata 編集、ファイル登録、並び替え、登録解除はこのファイルだけを更新します。Markdown 本文や frontmatter は自動で書き換えません。

最小例（v3）:

```json
{
  "version": 3,
  "books": [
    {
      "id": "book-main",
      "name": "本編",
      "authors": ["著者A"],
      "language": "ja",
      "writingMode": "vertical-rl",
      "items": [
        {
          "id": "chapter-001",
          "path": "本文/001_序章.md",
          "role": "body",
          "title": "序章",
          "authors": ["著者A"],
          "translators": []
        }
      ]
    }
  ],
  "materials": [
    {
      "id": "mat-001",
      "path": "資料/人物/藤崎美咲.md",
      "role": "character",
      "title": "藤崎美咲",
      "authors": [],
      "translators": []
    }
  ],
  "ignored": []
}
```

- `books[].name`: 作品タブに表示する Book 名です。
- `books[].items[]`: Book に属する本文ファイルです。並び順は配列順です。表示 title / authors / translators は `title` / `authors` / `translators` です。
- `materials[]`: 資料ファイルです。`role` は `character` / `setting` / `synopsis` / `material` / `unsorted` のいずれかです。表示 metadata も `title` / `authors` / `translators` です。
- `path`: project root からの相対 path です。登録済み path が存在しない場合も一覧に残り、missing 表示になります。missing 行は通常タブで開けず、本文は Outline / 前後章ナビの対象にもなりません。資料の missing 行は preview / 簡易編集も使えません。今できる整理は **登録解除** のみです（ファイルの再接続 UI は未実装）。

Mac と Windows の間では、日本語ファイル名の濁点 / 半濁点などが Unicode の別表現で保存されることがあります。Nyoze は `.nyoze/books.json` の `path` 文字列を勝手に正規化して書き換えず、ファイルを探すときだけこの差分を吸収します。曖昧に一致する候補が複数ある場合は、安全のため missing として扱います。

Books セクションでは Book ごとに本文ファイルを一覧表示し、章タイトルのクリックで通常タブへ開けます。Book が空でも Book 名、編集、登録解除、折りたたみ操作は表示されます。Materials セクションでは資料ファイルを 1 つの一覧にまとめ、`全て` / `人物` / `設定` / `梗概` / `資料` / `未整理` の chip で複数 role を同時に表示できます。`全て` は一括 ON/OFF のトグルです。資料名のクリックで下部 preview を切り替え、同じ資料をもう一度クリックするか索引の空白をクリックすると preview を解除できます。資料を通常タブで開く場合は、各項目の **中央で開く** を使います。現在ファイルは強調表示されます。

作品タブには、作品切り替え、Books / Materials / 未登録ファイルセクションの折りたたみ、Book ごとの折りたたみもあります。未登録ファイルセクションは `.nyoze/books.json` が **v3 ready** であるか、まだ未初期化（manifest absent・warning なし）の Project で表示されます（未登録ファイルが 0 件でも空状態として表示されます）。対象は project root 配下の `.md` / `.markdown` / `.txt` です。一覧から **Book に追加**（本文・章として登録）または **資料にする**（資料として登録）できます。Book がまだない場合は本文として追加できませんが、資料としての登録は可能です。manifest が破損している場合は未登録一覧も表示しません。

**作品登録を解除**、**Book / 本文 / 資料の登録解除** は、いずれも registry から外す操作です。Markdown 本文は削除されません。Book や本文 / 資料の登録解除後、project 内にファイルが残っていれば未登録ファイル一覧から再登録できます。ファイルが見つからない registry 行の登録解除は、見つからない登録だけを外します。作品登録解除は `.nyoze/project.json` と `.nyoze/books.json` だけを外し、`notes.json` も Markdown 本文も削除しません。未削除の付箋がある場合は作品登録解除できません。

`body` item だけを Book / Outline / 前後章ナビゲーション / 将来出力の対象として特別扱いし、Materials の `role` は資料一覧のサブタイプとして扱います。現時点では role ごとの専用編集 UI はなく、アイコンと filter に使います。

#### 資料 preview と簡易編集

資料ファイルを選ぶと、作品タブ下部に Markdown preview が表示されます。Notes preview と同様、単一改行は `<br>` として表示されます。raw HTML は実行されません。

資料には **Preview / Edit** 切替があり、Edit では横書き `textarea` で Markdown を簡易編集できます。**保存** / **キャンセル** を明示し、保存前に外部変更を検知します。未保存のまま別資料を選ぶ、再読み込みする、**中央で開く** を押すとブロックされます。

Books の本文章ファイルは右ペイン内編集の対象外です。**中央で開く** を使って通常の文書タブとして開いてください。

#### ページビューア

**File > ページビューア** または上部ツールバーの Page Viewer ボタンから、現在の文書を独立した読み取り専用ウィンドウで開けます。同時に開ける Page Viewer は 1 つだけです。すでに開いている場合は、そのウィンドウを前面に出して新しい対象へ切り替えます。

現在開いているファイルが `.nyoze/books.json` v3 に登録された Book の本文（body）章である場合、ツールバーの Page Viewer ボタンは split button になります。左側の Presentation アイコンは従来どおり現在の文書を開き、右側の▼から対象を選べます（現在の文書 / Book 全体）。Materials、未登録ファイル、作品外の単独ファイルでは、従来どおり単独の Page Viewer ボタンのままです。

- 現在の文書を開く場合は、起動時点の編集内容を snapshot として表示します。未保存の変更も表示されますが、Viewer 側から保存・編集はできません。
- 本文は CSS Columns でページ単位に組み、キーボード、下部 scrubber、目次、Outline パネルからページ移動できます。`:::page-break` は次の本文を新しいページから始め、`:::blank-page-N` は指定枚数の空白ページとして表示します。
- Markdown の見出し、強調、ルビ、TCY、リスト、引用、コード、ブロック装飾、許可された相対ローカル画像を読み取り専用で表示します。危険な画像 URL や読み込めない画像は安全な placeholder になります。
- 起動時点の Display Settings にある本文の書字方向、色、フォント、文字サイズ、行間、見出しのフォント・配置・後余白・区切り線、ルビの文字サイズ、および縦書き時の自動 TCY（`auto TCY` / 対象 / 桁数）を継承します。自動 TCY は表示専用で、Viewer を開いた時点の設定を snapshot として使い、横書き表示では効きません。文書冒頭情報の表示（`frontmatterVisible`）と著者・訳者行 / 役割ラベルの表示可否も、起動時点の Display Settings を snapshot として反映します（専用の Viewer 設定はまだありません）。Viewer 内の書字方向・Reader theme の切替は、その Viewer ウィンドウだけに適用されます。Display Settings の変更を Viewer へライブ同期する機能や、Viewer 固有設定の永続化はまだありません。ルビの位置（縦横のオフセット）は対象外です。
- Viewer 上部 header の **ページ遷移アニメーション** ボタンから、ページ移動時のアニメーションを **なし / フェード / スライド / ズーム** の 4 種類から選べます（既定はフェード）。同じメニュー内に **速度** スライダー（200〜1000 ミリ秒、50 ミリ秒刻み、既定 500 ミリ秒）があり、フェード・スライド・ズームの 3 種類すべてに共通の速さとして適用されます（「なし」を選んでいる間はスライダーを操作できません）。速度を変更しても、実行中のアニメーションが途中で作り直されることはなく、次のページ移動から新しい速さが使われます。スライドは、本文そのものを動かすのではなく、縦書き・横書きのどちらの文書でも左右方向へ、ページの縁を思わせる淡い帯がごく短く通過する演出です（縦書きでは進む方向が左→右、戻る方向が右→左、横書きではその逆になります）。ズームはわずかな拡大とフェードを組み合わせます。選択したアニメーションと速度はその Viewer ウィンドウ内だけの一時設定で、Viewer を閉じる、または同じウィンドウで別の対象を開くとフェード・500 ミリ秒へ戻ります。OS の「視差効果を減らす」（`prefers-reduced-motion`）が有効な場合は、選択にかかわらずアニメーションしません。現在の文書 / Book 全体のどちらの Viewer でも同じように使えます。
- Viewer 上部 header の歯車アイコンから **Page Viewer Settings** を開けます。**本文・ページ**では **上余白**（既定 24px）/ **下余白**（既定 8px）/ **左右余白**（既定 16px）（いずれも 0〜80px、8px 刻み）と **用紙枠**（既定 ON）を設定できます。余白を0pxにしても、画像や文字の端切れを避ける最小の上/下8pxだけは残ります（左右は0px）。したがって既定の実効本文余白は上32px・下16px・左右16pxです。用紙枠 ON のときは、header帯を避けて少し下げた紙面の外側にcanvas余白と枠線・影が出ます。**ヘッダー** / **フッター**では読書面の見出し帯とページ番号帯を設定できます。ヘッダーは表示（既定 ON）・位置（左 / 中央 / 右、既定 左）・内容（タイトル / タイトルと著者、既定 タイトル）を選べます。タイトル・著者は起動時点の文書情報（単独文書）または Book 情報（Book 全体）から取り、Display Settings の文書冒頭情報表示（`frontmatterVisible`）が OFF のときは出ません。著者は内容が「タイトルと著者」かつ著者表示が ON のときだけ出ます。訳者・章タイトル・ウィンドウタイトルはヘッダーに使いません。左寄せでは macOS の信号ボタン、右寄せでは Windows / Linux のウィンドウ操作ボタンと重ならないよう、**用紙枠が OFF のときだけ**余白帯内で自動的に内側へ寄せます（用紙枠 ON のときは紙面自体がウィンドウ操作帯の外にあるため、追加の寄せはしません）。フッターは表示（既定 ON）・位置（左 / 中央 / 右、既定 右）だけで、表示内容は常に「現在ページ / 総ページ数」（1 始まり、空白ページも含む全体の通し番号）です。ヘッダー / フッターの切替はページ組みを再計測せず、余白帯の上に重ねて表示するだけです。**ページ構成**では **見出しの前で改ページ**（チェックボックス、既定 OFF）と、有効なときだけ操作できる **対象見出し**（H1のみ 〜 H1〜H6、既定 H1のみ）のプルダウンを設定できます。ここで変更した内容は、その Viewer ウィンドウへ即座に反映されるだけでなく、ページ遷移アニメーション設定とは異なり **アプリの設定として保存**され、次に開く Page Viewer（現在の文書 / Book 全体のどちらも）にも引き継がれます。すでに開いている別の Page Viewer ウィンドウには、そのウィンドウ自身で変更しない限り自動的には反映されません。余白や用紙枠を変えるとページ組みが再計測され、変更前の読書位置の比率をできるだけ保ちます。この設定は本文 Markdown・frontmatter・`.nyoze/books.json` を一切変更しません。Book 全体を表示する場合も、既存の章境界の改ページ（章の先頭で新しいページが始まる既存の挙動）とは独立して働くため、章の途中にある見出しの前でも追加の改ページが入ります。外部書き出し（青空文庫風 TXT、LeME、でんでん、Web Book）の「見出しの前で改ページ」設定とは別物で、値は共有されません。
- header のピンアイコンでは、そのViewerだけheaderの自動非表示を固定／解除できます。固定状態は保存されず、Viewerを閉じると元に戻ります。
- **簡易表紙**では、文書情報（Book 全体では作品情報）を冒頭の独立した1ページとして表示できます（既定 OFF）。表紙の書字方向は本文に従う / 縦書き / 横書き、配置は通常 / 中央から選べます。値は Page Viewer Settings として保存され、Web Book の書き出し設定とは共有しません。title と表示許可された著者だけを使い、訳者・章情報・ウィンドウタイトルは表紙に出ません。文書冒頭情報の表示がOFFなら表紙も出ません。

#### Book 全体 Page Viewer

現在開いているファイルが `.nyoze/books.json` v3 に登録された Book の本文（body）章であれば、**File > Book 全体をページビューアで開く...**、またはツールバー Page Viewer の▼メニューから **Book 全体をページビューアで開く** で、その Book 全体を独立した読み取り専用 Page Viewer ウィンドウで開けます。

- 対象は現在ファイルが属する Book の本文章すべてです。Materials、未登録ファイル、作品外の単独ファイルでは実行できません。
- 表示 metadata は `.nyoze/books.json` v3 の Book 名 / 著者と、各章の title / authors / translators を使います。章ファイルの frontmatter `title` / `author` / `translator` へは fallback しません。
- 章ファイルはディスク上の保存済み内容を read-only に読みます。未保存の編集がある場合は反映されない旨の通知を出しますが、自動保存や書き換えは行いません。
- Book 冒頭情報、目次、各章冒頭情報を Page Viewer 内に表示します。章境界は既定で改ページされ、本文中の `:::page-break` / `:::blank-page-N` も Page Viewer の RenderModel と同じ意味で反映されます。各章冒頭情報（chapterInfo）は Display Settings の **作品内ファイルのタイトル・著者も表示**（`frontmatterShowInProjectFiles`、既定 OFF）が ON のときだけ出ます。タイトル / 著者の個別表示も同セクションの設定に従います。Book 冒頭情報と単独文書の冒頭情報には影響しません。
- 起動時点の Display Settings にある色、フォント、文字サイズ、行間、見出しのフォント・配置・後余白・区切り線、ルビの文字サイズ、縦書き時の自動 TCY、文書冒頭情報 / 著者・訳者行 / 役割ラベルの表示可否、および作品内ファイル向けの章冒頭情報表示可否は、現在の文書の Page Viewer と同じ appearance / metadata visibility snapshot を Book 全体へ引き回します（`.nyoze/books.json` v3 の `writingMode` は書字方向だけを上書きします）。章の表示 metadata の値は books.json v3 が正本です。
- Book を開く場合も、現在の Page Viewer ウィンドウを再利用します。Book の各章は保存済みディスク内容を読むため、現在編集中の章に未保存変更がある場合は保存してから開いてください。
- `.nyoze/books.json` が未初期化または不正、対象 Book が見つからない、本文章が 0 件、章ファイルが missing / read-error の場合は、部分的な viewer は開かず通知します。

#### frontmatter の正本

key の詳細・YAML 制限・legacy 互換の一覧は [`docs/frontmatter-reference.md`](docs/frontmatter-reference.md) を参照してください。

作品内の Book 所属、章順、資料 role、作品タブ・文書冒頭・左ペインで使う **表示 metadata** の正本は `.nyoze/books.json` **v3** です。`.nyoze/project.json` は作品タイトルなどの Project metadata を保持しますが、章順や Book 所属の source of truth ではありません。

右ペイン **Document Metadata** で編集する `title` / `author` / `translator` は **frontmatter** の正本です。作品内の登録済みファイルでは、Document Metadata の frontmatter 編集と 作品タブの books.json 編集は **自動同期しません**（詳細は [文書メタデータ（限定編集UI）](#文書メタデータ限定編集ui)）。

Markdown frontmatter は、作品外の単独文書では従来どおり `title` / `author` / `translator` / `documentType` / `writingMode` などの文書 metadata として使えます。作品内の章ファイルや資料ファイルでも任意の frontmatter を書いて構いませんが、Nyoze の Book 構造は `book` / `order` / `role` frontmatter に依存しません。これらの key は未リリース移行期の案であり、正式な Project / Book 管理の正本ではありません。

#### まだできないこと

- 章順のドラッグ変更
- Book / Project 全体の詳細設定 UI（出力設定の永続化、著者情報テンプレート生成など）
- EPUB / PDF の本体直接生成、Vivliostyle 書籍モード
- Book / Project 全体の文字数、未完了付箋数の集計

### アウトライン（Outline）タブ

右ペインの **Outline** タブ上部には **現在の文書** / **Book全体** の切替があります。既定は **現在の文書** です。

#### 現在の文書

開いている文書内の見出し一覧です。見出しの折りたたみ、プレビュー、クリックによるジャンプなど、従来どおり使えます。

#### Book全体

現在ファイルと同じ `book` に属する `role: body` の章ファイルを、章順に並べて見出しを read-only 表示します。各章ファイルは、章タイトルを見出し代わりの **章 root 行**として表示し、章内の見出しはその下にインデントして並びます。

- 章 root 行・各見出しの左にある **▸ / ▾ トグル**で折りたたみできます。章 root を畳むとその章の見出しが全て隠れ、見出しを畳むとその配下の小見出しだけが隠れます（単文書 Outline と同じ操作感）。この折りたたみは右ペインの表示だけを畳むもので、中央の本文や保存内容には影響しません。開閉状態は保存されません。
- 現在開いている章の root 行と、現在ファイル内でキャレットが入っている見出しに対応する行が強調表示されます（現在位置ハイライト）。対応見出しの照合は best-effort で、複雑な文書では章 root だけが強調されることがあります。
- 章 root 行・各見出しの右にある **吹き出しアイコン**にマウスを乗せると、本文の冒頭プレビューが表示されます。章 root は**その章ファイルの本文冒頭**、見出しは**その見出し直後の本文冒頭**の短い抜粋（最大 3 行）を表示します。行を**右クリック**しても同じプレビューを開け（もう一度右クリックで閉じます）、単文書 Outline のプレビューと同じ操作感です。プレビューは表示専用で本文・frontmatter を変更しません。本文が無い見出し・章ではアイコンが出ません。

章 / 見出し / 前後章のナビゲーションは、**通常クリックでは同じタブの中で章ファイルを切り替え**ます。細切れの章ファイルを、あたかも一つのファイルのように順番に行き来できます。**Shift を押しながらクリック**すると、対象の章を別タブで開きます。

- **章タイトル** をクリック: 別ファイルなら同じタブに切り替え（Shift+クリックで別タブ）、同じファイルなら章先頭付近へ移動します
- **見出し** をクリック: 同じファイルならその見出しへ best-effort でジャンプします。別ファイルなら同じタブに読み込んだあと（Shift+クリックで別タブに開いたあと）best-effort でジャンプします
- **前章** / **次章**: 同一 Book 内の隣接 body 章へ移動します（Book全体モードのツールバー）。通常クリックで同じタブ、Shift+クリックで別タブ

同一 Book 内の **前章** / **次章** は、エディタ上部のツールバー（保存ボタンの右隣）からも移動できます。Outline タブを開かなくても、本編の章ファイルを順番に行き来できます。こちらも通常クリックは同じタブ、Shift+クリックは別タブです。現在ファイルが Book の body 章でない場合（資料ファイル・Book 未指定・作品外など）や `Source Mode` / `Paragraph Plain` 編集中はボタンが無効になります。

本編（中央エディタ）を**章の先頭付近までスクロールすると「前章の末尾へ」「章の末尾へ」**、**末尾付近までスクロールすると「章の先頭へ」と「次章へ」**のボタンが本文の上に表示されます。Outline タブやツールバーを使わなくても、本文を読み進めながらそのまま隣の章へ移動できます。

- **前章の末尾へ** / **次章へ**: 通常クリックで同じタブ、Shift+クリックで別タブに開きます（ツールバーや Outline の前後章と同じ動作です）。
- **章の先頭へ** / **章の末尾へ**: 現在の章・現在のタブのまま、本文の論理的先頭または末尾へスクロールするだけです（ファイルの再読込やタブ切替は行いません。Shift を押しても別タブには開きません）。

横書きでは上下、縦書きでは左右の端に表示されます。境界付近へスクロールしたときに一時的に表示され、しばらく操作がなければ自動的に消えます。対応する端付近へポインターを動かすと、再び表示されます。**前章の末尾へ** / **次章へ**は、該当する隣接章がある場合のみ表示されます（章頭・章末の「章の末尾へ」「章の先頭へ」は隣接章がなくても、スクロール位置が条件を満たせば表示されます）。現在ファイルが Book の body 章でない場合や `Source Mode` / `Paragraph Plain` 編集中は、章境界ナビ全体が表示されません。短い文書で先頭と末尾が同時に近い場合は、「章の先頭へ」「章の末尾へ」は表示されません（すでにその端にいるため）。

同じタブで切り替えるときも、未保存の変更がある章では従来どおり保存確認が出ます（破棄せずに移動を中止できます）。同じファイルが別タブですでに開いている場合は、通常クリックでもそのタブに切り替わります。

**Option / Alt + スクロールでも前後章へ移動できます。** Book 本文の章頭付近で `Option/Alt + 前方向スクロール` すると前章の末尾へ、章末付近で `Option/Alt + 次方向スクロール` すると次章の先頭へ、いずれも同じタブで移動します（ボタンと同じ移動です）。誤操作を防ぐため、1 回の軽いスクロールでは発動せず、同じ方向へ少しスクロールを続けると移動します。連続して何章も飛ばないよう、1 回の移動後はしばらく次の移動を受け付けません。`Source Mode` / `Paragraph Plain` 編集中・日本語入力（変換）中・資料ファイル・作品外の単独ファイルでは無効です（`Shift` / `Ctrl` / `Cmd` と同時押しのスクロールでも移動しません）。

見出しジャンプは Markdown 上の見出しテキストと level を照合する best-effort 方式です。複雑な文書ではずれることがあります。`Source Mode` または `Paragraph Plain` 編集中は、章 / 見出し / 前後章のナビゲーションは無効になります。

Book全体 Outline は 作品タブの Books 一覧とは別機能です。Books は作品全体の索引、Book全体 Outline は同一 Book の目次として使います。

#### まだできないこと

- 前後章のグローバルショートカット（ツールバーの常設ボタン、章頭 / 章末のオーバーレイボタンは利用できます）
- スクロールだけで章境界を越える連続表示（複数章を一つの仮想文書として結合する表示）
- 厳密な Markdown source map による見出しジャンプ
- Book 全体の文字数表示

### ルビ・傍点ダイアログ（統合UI）

**Ruby** ボタンを押すと、選択テキストに対する統合ダイアログが開きます。
ダイアログ上部のタブで「ルビ」と「傍点」を切り替えます。

#### ルビタブ

- テキスト入力欄にルビ（読み仮名）を入力して OK で確定
- 青空文庫記法（`｜text《ruby》`）として挿入される
- 既存のルビがある場合は編集用に読み仮名が初期表示される
- 空欄で OK → ルビを除去（本文テキストのみ残す）

#### 傍点タブ

- 傍点文字をプルダウンから選択して OK で確定
- 既定候補: `・ • ● ○ ⚬ ◦ ﹅ ﹆`
- 「任意の傍点文字を1文字入力」欄から候補を追加できる（ローカルストレージに保存）
- 各文字ごとにルビ記法（`｜字《・》`）として展開される

### キーボード操作（通常編集）

通常編集中は以下のショートカットが有効です（macOS / Windows）:

#### 装飾・編集

| キー                        | 機能                                           |
| --------------------------- | ---------------------------------------------- |
| `Cmd/Ctrl + B`              | 太字                                           |
| `Cmd/Ctrl + I`              | 斜体                                           |
| `Cmd/Ctrl + Shift + X`      | 取り消し線                                     |
| `Cmd/Ctrl + K`              | リンク設定                                     |
| `Cmd/Ctrl + Alt/Option + R` | ルビ / 傍点ダイアログを開く                    |
| `Cmd/Ctrl + Shift + C`      | 書式クリア（選択範囲の全インライン装飾を解除） |
| `Cmd/Ctrl + Z`              | Undo                                           |
| `Cmd/Ctrl + Shift + Z`      | Redo                                           |

#### 見出し

| キー                 | 機能                          |
| -------------------- | ----------------------------- |
| `Cmd/Ctrl + Alt + 1` | 見出し 1 に切り替え（トグル） |
| `Cmd/Ctrl + Alt + 2` | 見出し 2 に切り替え（トグル） |
| `Cmd/Ctrl + Alt + 3` | 見出し 3 に切り替え（トグル） |
| `Cmd/Ctrl + Alt + 0` | 段落に戻す                    |

#### モード切替

| キー                        | 機能                                                   |
| --------------------------- | ------------------------------------------------------ |
| `Cmd/Ctrl + Alt/Option + P` | Paragraph Plain モードのトグル（Source Mode 中は無効） |

#### アウトライン・折りたたみ

| キー                   | 機能                                                       |
| ---------------------- | ---------------------------------------------------------- |
| `Cmd/Ctrl + Shift + ,` | 横書き: 前の見出し／縦書き: 次の見出し（視覚方向に合わせる） |
| `Cmd/Ctrl + Shift + .` | 横書き: 次の見出し／縦書き: 前の見出し（視覚方向に合わせる） |
| `Cmd/Ctrl + Shift + L` | 現在の見出しを折りたたみ/展開（トグル）                     |

> **注**: 縦書きでは `,` / `.` の視覚的な向きに合わせて `前` / `次` の意味が入れ替わります。キー判定は `event.code` ベース（`Comma` / `Period` / `KeyL`）なので、日本語配列や Shift 合成で `<` / `>` に化ける環境でも安定します。fold に `L` を当てているのは、以前採用していた `Cmd/Ctrl+Shift+/` が macOS の Help 検索と衝突して実用にならなかったためです。

#### ペイン開閉

| キー                        | 機能                                  |
| --------------------------- | ------------------------------------- |
| `Cmd/Ctrl + Alt/Option + ,` | 左ペイン（File Explorer）の開閉       |
| `Cmd/Ctrl + Alt/Option + .` | 右ペイン（Outline / Document 等）の開閉 |

> **注**: ペイン開閉は編集コマンドではないので、`Paragraph Plain` / `Source Mode` 中でも利用できます。判定は `event.code` ベース（`Comma` / `Period`）です。`,` / `.` は見出しナビ（`Cmd/Ctrl+Shift+,` / `.`）とも近いため、ペイン開閉は `Alt/Option` 併用で区別します（Shift を押さないで `Alt/Option` だけ押す）。以前採用していた `Cmd/Ctrl+Alt+[` / `]` は、日本語配列で実機 `Cmd+Alt+[` が成立しないケースが多かったため廃止しました。

#### リスト移動

| キー                    | 機能                                                         |
| ----------------------- | ------------------------------------------------------------ |
| `Cmd/Ctrl + ArrowUp`    | （横書き）リスト項目を上（文書先頭方向）へ移動（同一階層内） |
| `Cmd/Ctrl + ArrowDown`  | （横書き）リスト項目を下（文書末尾方向）へ移動（同一階層内） |
| `Cmd/Ctrl + ArrowRight` | （縦書き）リスト項目を上（文書先頭方向）へ移動（同一階層内） |
| `Cmd/Ctrl + ArrowLeft`  | （縦書き）リスト項目を下（文書末尾方向）へ移動（同一階層内） |
| `Tab`                   | リスト項目を1段深くする（ネスト）                            |
| `Shift + Tab`           | リスト項目を1段浅くする（アンネスト）                        |

> **注**: リスト外では発火しません。IME composition中・Paragraph Plain / Source Mode中も無効です。

#### カーソル移動

| キー       | 機能                                                         |
| ---------- | ------------------------------------------------------------ |
| `Home`     | 1回目: 表示行頭へ移動、2回目: 論理行頭（ブロック先頭）へ移動 |
| `End`      | 1回目: 表示行末へ移動、2回目: 論理行末（ブロック末尾）へ移動 |
| `PageUp`   | 1ページ前へ移動し、キャレットも移動先の表示範囲へ追従        |
| `PageDown` | 1ページ先へ移動し、キャレットも移動先の表示範囲へ追従        |

> **注**: 修飾キー付き（`Shift+Home` 等）はブラウザ標準の挙動です。IME composition中は標準挙動に委ねます。Source Mode / Paragraph Plain では各エディタ標準の挙動です。

#### 検索・置換

| キー           | 機能                 |
| -------------- | -------------------- |
| `Cmd/Ctrl + F` | 検索バーを開く       |
| `Cmd/Ctrl + H` | 検索・置換バーを開く |

### ショートカットキー一覧の内蔵ドキュメント

Help メニューの **「ショートカットキー一覧」** を選ぶと、現在の UI 言語に応じた read-only の内蔵 Markdown 文書が新しいタブで開きます。

- `ja` / `mixed`: 日本語版
- `en`: 英語版
- すでに開いている場合は、同じタブを再利用
- 横書き固定
- 読み物用の internal doc なので保存・編集はできない

この internal doc では、通常文書向けの編集導線を意図的に閉じています。

- **Document Metadata** は使えません
- ツールバー / 右クリックメニュー / 編集系ショートカットは無効です
- 検索、コピー、スクロール、アウトライン移動だけ使えます
- `Cmd/Ctrl + H` は置換ではなく検索のみ開きます

#### 基本操作

| キー            | 機能                                                                                                                     |
| --------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `Enter`         | 段落を分割                                                                                                               |
| `Shift + Enter` | 小説向けの通常本文（paragraph / heading）では無効。リスト・引用などの構造ブロック内では hardBreak（`  \n`）                  |
| `Backspace`     | 前の文字を削除、またはブロックをマージ                                                                                   |
| 矢印キー        | カーソル移動                                                                                                             |

> **注**: ルビや明示 TCY（縦中横）の直後で日本語 IME 入力が、まれに 1 タイプ目の直後に 2 タイプ目で詰まることがあります。その場合は `Escape` を押すと未確定入力を破棄して通常の編集状態へ復帰できます。

---

## 検索・置換

`Cmd/Ctrl + F` で文書内検索バーが開きます。`Cmd/Ctrl + H` で置換欄も同時に表示されます。

### 検索操作

| 操作                      | 動作                                                           |
| ------------------------- | -------------------------------------------------------------- |
| テキスト入力              | 入力内容を更新（検索実行は `Enter` / 検索ボタン / 前後移動時） |
| `Enter`                   | 次の一致へ移動                                                 |
| `Shift + Enter`           | 前の一致へ移動                                                 |
| `Escape`                  | 検索バーを閉じる（ハイライト解除）                             |
| Aa ボタン（大文字小文字） | 大文字小文字の区別を切り替える                                 |

### 置換操作

| 操作         | 動作                               |
| ------------ | ---------------------------------- |
| 置換ボタン   | 現在の一致を置換テキストに置き換え |
| 全置換ボタン | すべての一致を一括置換             |

### 注意事項

- 件数表示: 「現在位置 / 全件数 件」が検索バーに表示されます
- 折りたたまれた見出しの中にマッチがある場合、移動時に自動展開されます
- IME 入力中（日本語変換中）は置換操作が抑止されます
- 検索入力欄/置換入力欄では、IME 文字確定の `Enter` は検索/置換を発火しません
- 検索は表示テキストに対して行われます（Markdown 記法は検索対象外）

---

## Paragraph Plain モード（段落単位ソース編集）

フォーカスされているブロックの上に textarea オーバーレイを表示し、そのブロックの Markdown ソースを直接編集するモードです。

**表示設定** の **Paragraph Plain** セクションで、アプリ全体の **Paragraph Plain の動作**（軽さ優先 / 表示優先）を切り替えられます。既定は軽さ優先です。詳細は下記「表示設定」の表を参照してください。文書には保存されません。

### 対応ブロック型

| ブロック型        | 表示例                            |
| ----------------- | --------------------------------- |
| `paragraph`       | 通常の段落                        |
| `heading`         | 見出し（`# H1`〜`###### H6`）     |
| `codeBlock`       | コードブロック（` ``` `）         |
| `html_block_atom` | HTML ブロック                     |

> **注意**: `blockquote` はコンテナ型のため、Paragraph Plain の直接対象にはなりません。
> blockquote の内側にある `paragraph` や `heading` が対象です。
> リスト項目は beta では Paragraph Plain の直接対象外です。リストの編集は通常編集または Source Mode を使ってください。

### モードの有効化・無効化

- ツールバーの **`Paragraph Plain`** ボタンをクリック（トグル）
- キーボードショートカット **`Cmd/Ctrl + Alt/Option + P`** でも有効化・無効化できます（Source Mode 中は無効）
- Escape キーでも無効化できます

### キーボード操作（Paragraph Plain モード）

**`paragraph` / `heading` ブロックで有効なキー:**

| キー                        | 条件                                  | 動作                                                                                                    |
| --------------------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `Enter`                     | 修飾キーなし、IME未確定でない         | カーソル位置でブロックを分割する。前半は単一 Markdown block として再解釈できる場合はその型で確定し、後半は paragraph として続ける |
| `Backspace`                 | カーソルが先頭（位置0）で選択範囲なし | 直前の textblock（paragraph / heading）とマージする。マージ点にカーソルを置く                           |
| `ArrowLeft`                 | カーソルが末尾                        | 次のブロック（paragraph / heading）に移動する                                                           |
| `ArrowRight`                | カーソルが先頭                        | 前のブロック（paragraph / heading）に移動する                                                           |
| `Shift + Enter`             | `paragraph` / `heading`               | 無効（段落内改行を挿入しない）                                                                          |
| `Cmd/Ctrl + Enter`          | 常時                                  | 無効（textarea の改行 fallback を防ぐためブロックされます）                                             |
| `Cmd/Ctrl + Alt/Option + P` | 常時                                  | 編集内容を確定してモードを終了する                                                                      |
| `Escape`                    | 常時                                  | 編集内容を確定してモードを終了する                                                                      |

> **矢印キーのブロック間移動（writing-mode依存）**
>
> - **縦書き**（vertical-rl）: `ArrowLeft`（末尾 → 次のブロック）、`ArrowRight`（先頭 → 前のブロック）
> - **横書き**（horizontal-tb）: `ArrowDown`（末尾 → 次のブロック）、`ArrowUp`（先頭 → 前のブロック）

**`codeBlock` / `html_block_atom` ブロック:**

これらのブロック型では Enter・Shift+Enter・Backspace・矢印キーは textarea のデフォルト動作になります（複数行コンテンツが想定される型のため）。
`Cmd/Ctrl + Alt/Option + P` または `Escape` でモード終了は共通です（どちらも編集内容を確定します）。

### 注意事項

- `paragraph` / `heading` では、単一ブロックの範囲なら `paragraph` ↔ `heading` の切り替えも確定できます。たとえば heading の `## ` を削除して段落へ戻す、paragraph の先頭に `## ` を付けて見出し化する、といった編集が可能です。
- `paragraph` / `heading` を編集中に `# 見出し`、`- list`、`1. list`、`> quote`、fenced code、`---` などを入力して Paragraph Plain を終了した場合は、単一 top-level Markdown block として解釈できる範囲で通常表示側の見出し・リスト・引用・コードブロック・区切り線へ反映されます。ツールバーボタン / ショートカット / Escape による終了だけでなく、境界矢印で隣接 block へ移る場合や `Enter` 分割の前半 block でも同じです。
- 複数ブロック化（例: `# 見出し` + 空行 + 本文）や、Paragraph Plain の直接対象外 block からの大きな構造変更は beta では対象外です。まとまった構造変更は通常 WYSIWYG か Source Mode を使ってください。
- `paragraph` / `heading` 以外のブロックで Enter を押すと textarea に改行が挿入されます。`codeBlock` なら複数行コードとして正常に確定できます。
- IME 変換中（日本語入力中）は Enter・Backspace・矢印の特殊動作は無効になります。変換確定後に通常通り動作します。

---

## Source Mode（全文ソース編集）

文書全体を 1 つの横書き source editor に Markdown テキストとして表示し、直接編集するモードです。frontmatter を含む全文を raw のまま編集できます。

> beta 版の Source Mode は raw save 専用導線ではありません。`Cmd/Ctrl + Enter` や Save 時には Markdown を Nyoze の文書モデルへ読み込み、保存時に Nyoze の Markdown 表現として出力します。GFM table、reference-style link、footnote、definition list など、Nyoze が完全保持しない構文は正規化される場合があります。

### キーボード操作（Source Mode）

| キー                   | 動作                                                 |
| ---------------------- | ---------------------------------------------------- |
| `Cmd/Ctrl + Z`         | Source editor の Undo                                |
| `Cmd/Ctrl + Shift + Z` | Source editor の Redo                                |
| `Cmd/Ctrl + A`         | Source editor 全文選択                               |
| `Cmd/Ctrl + Enter`     | 編集内容を Markdown として適用し、WYSIWYG 表示に戻る |
| `Escape`               | 編集内容を破棄して WYSIWYG 表示に戻る                |

- Source Mode は `writingMode` に関わらず **横書き固定** です。
- Source Mode 有効中は、ツールバーの **`Paragraph Plain`** ボタンは無効（グレーアウト）になります。

### Source Mode で空行が意味を変える場面

Source Mode は raw Markdown を直接編集するため、WYSIWYG では自動的に保たれる block 境界も、自分で Markdown ルールどおりに書く必要があります。
特に次のケースでは、空行の有無で構造が変わります。

#### 1. 通常段落を次の通常段落へ分ける

空行がないと、次の行は同じ paragraph の続きです。

```md
1段落目

2段落目
```

#### 2. リストの外に戻って通常段落を続ける

空行がないと、次の行は「リストの次の段落」ではなく、同じ list item の continuation として解釈されます。

```md
- 箇条書き項目

通常段落
```

#### 3. 同じ list item の中で段落を分ける

同じ list item の中で 2 段落にしたい場合は、空行に加えて継続段落としてのインデントが必要です。

```md
- 1段落目

  2段落目
```

#### 4. blockquote の外に戻って通常段落を続ける

空行がないと、次の行は blockquote の lazy continuation として解釈されることがあります。

```md
> 引用です。

通常段落です。
```

#### 5. 同じ blockquote の中で段落を分ける

blockquote 内で段落を分けるときは、空行を入れつつ `>` を維持します。

```md
> 1段落目
>
> 2段落目
```

#### 実用上の目安

- major block（通常段落 / list / blockquote）を切り替えるときは、迷ったら 1 行空けるのが安全です。
- Source Mode では CommonMark の解釈がそのまま効くため、空行を削ると見た目どおりの block 構造にならないことがあります。
- 特に `list -> 通常 paragraph` と `blockquote -> 通常 paragraph` は、空行なしだと意図と違う構造になりやすい代表例です。

---

## マウスホイール・スクロール

縦書きモードではスクロール軸が横方向（scrollLeft）になります。横書きモードではブラウザ標準の縦方向スクロールです。

| 操作                    | 動作                                         |
| ----------------------- | -------------------------------------------- |
| ホイール下（deltaY 正） | 順方向スクロール（読み進む方向：右側の列へ） |
| ホイール上（deltaY 負） | 逆方向スクロール（前へ戻る方向）             |
| `Shift` + ホイール      | 縦方向スクロール（ブラウザデフォルト）       |

> トラックパッドの場合、横スワイプ（deltaX）もそのまま順/逆方向スクロールに加算されます。
> 拡大表示や狭いウィンドウで上部ツールバーの一部が隠れる場合は、ツールバー上でホイール / トラックパッド操作をすると左右へ移動できます。移動方向は縦書き本文のスクロール感覚に合わせています。

### macOS の Arrow キー移動時スクロール補正

macOS の Chromium / Electron 系環境では、通常のキャレット移動でも、`ArrowUp` / `ArrowDown` / `ArrowLeft` / `ArrowRight` でキャレットが表示端に達した瞬間に、ブラウザ標準の caret reveal によって半画面前後の大きなスクロールジャンプが起こることがあります。

Nyoze では、通常編集（WYSIWYG）と Source Mode の両方で、この **過大ジャンプだけを弱める補正**を既定で有効にしています。

- 対象は **macOS 上の bare Arrow** による通常のキャレット移動だけです
- Typewriter Mode が ON のときはこの補正は無効になります
- Home / End / PageUp / PageDown、選択中、IME 変換中、手動スクロール直後などには介入しません
- 補正は「理想的な 1 行送りを再現する」ものではなく、**半画面ジャンプを実用上許容しやすい量まで縮める**ことを目的にしています

環境によっては、完全に 1 行ずつにはならず、2〜3 行程度まとまって動く場合があります。これは native 挙動へ深く介入しすぎて別の回帰を起こさないための安全側の設計です。

---

## 表示設定

ツールバーの **`View Settings`** ボタンから設定ダイアログを開きます。

表示設定ダイアログの主な項目は、現在次のとおりです。

### 基本設定

| 設定項目       | 説明             | 既定値 |
| -------------- | ---------------- | ------ |
| フォントサイズ | 本文の文字サイズ | `20px` |
| 行間           | 本文の行間       | `1.9`  |

### TCY

| 設定項目        | 説明                                                 | 既定値 |
| --------------- | ---------------------------------------------------- | ------ |
| `auto TCY`      | 縦書き通常編集で、短い英数字を表示上だけ縦中横にする | `OFF`  |
| `auto TCY 対象` | 英字混じりを除外し、数字だけを自動 TCY 対象にする    | `OFF`  |
| 最小桁数        | 自動 TCY の対象にする最小桁数                        | `2`    |
| 最大桁数        | 自動 TCY の対象にする最大桁数                        | `4`    |

### Typewriter Mode

Typewriter Mode は、通常編集（WYSIWYG）でキャレット位置を追従帯の範囲に保つスクロール補助です。文書内容ではなく **編集環境の設定** なので、frontmatter ではなく `settings.json` に保存されます。

| 設定項目     | 説明                                                                                         | 既定値 |
| ------------ | -------------------------------------------------------------------------------------------- | ------ |
| `Typewriter scroll` | 通常編集中、キャレットが追従帯の外へ出たときだけスクロールを追従させます                         | `OFF`  |
| `Follow position`   | 画面中央からの相対位置です。正の値ほど進行方向寄り、負の値ほど手前側へ寄ります                     | `0%`   |
| `Follow band width` | 追加スクロールしない追従帯の幅です。狭いほど追従が早く、広いほど jump しにくくなります              | `16%`  |

補足:

- Typewriter は **通常編集（WYSIWYG）専用** です。`Source Mode` と `Paragraph Plain` 中は一時停止し、通常編集へ戻ると保存済み設定で再開します。
- bare Arrow、通常入力、IME の通常入力では追従します。
- `Home` / `End` / `PageUp` / `PageDown`、outline jump、検索 jump、遠距離 click、fold toggle、viewport restore の直後は、移動先の文脈を優先するため即座には再中央寄せしません。次の通常入力や bare Arrow で追従が再開します。
- 文書末尾では Typewriter 用の scroll past end が自動で有効になり、末尾テキストを 1〜2 行程度残したまま追従位置を確保します。
- **Visual Focus**（編集ブロックのハイライト、非フォーカス減光、**現在行ハイライト**の見た目など）は、同一の「Typewriter Mode」表示設定セクション内で Typewriter scroll と並列にまとまっています。ツールバーの **Typewriter**（タイプライターアイコン）から、`Typewriter scroll` / `Current line highlight` / `Edit block highlight` / `Dim non-focused blocks` をすばやく切り替えられます（詳細・スライダー類および現在行の色・透明度は表示設定が正本）。メニュー最下部の **`表示設定で詳細を編集…`** から、そのまま `Display Settings > Typewriter Mode` を開けます。「現在行」は折り返し後の **表示行** 単位です。横書きでは横に伸びる 1 行、縦書きでは現在の表示行に沿った **縦長の 1 本の帯** として表示します。IME 入力・変換中も、編集ブロックハイライトと同様に表示を維持します。

### フォント

| 設定項目             | 説明                                                      | 既定値   |
| -------------------- | --------------------------------------------------------- | -------- |
| 文書フォント         | 本文の書体を切り替える                                    | `明朝体` |
| カスタムフォント     | フォント名を登録し、文書フォントなどで選べるようにする    | なし     |
| システムフォント取得 | OS に入っているフォント一覧を取得し、登録候補として出す   | 未取得   |

### ルビ

| 設定項目           | 説明                         | 既定値   |
| ------------------ | ---------------------------- | -------- |
| ルビサイズ         | ルビ文字の大きさ             | `0.5em`  |
| ルビ位置オフセット | ルビを上下方向へ微調整する   | `0em`    |

### 見出し設定

| 設定項目               | 説明                                                 | 既定値                        |
| ---------------------- | ---------------------------------------------------- | ----------------------------- |
| 見出しフォント         | H1〜H6 の書体を指定する                              | `本文と同じ`                  |
| 見出し色               | H1〜H6 の文字色                                      | 文書テーマに連動              |
| 見出し位置             | H1〜H6 の位置を揃える                                | `左` / `上`                   |
| 見出し後マージン       | 見出し直後の余白                                     | `0.45em`                      |
| 見出し区切り線（レベル別） | レベルごとに区切り線の表示/非表示を切り替える     | `H1/H2=ON`, `H3-H6=OFF`       |

### 余白設定

| 設定項目 | 説明                   | 既定値              |
| -------- | ---------------------- | ------------------- |
| 上余白   | 本文先頭側の余白       | `0px`               |
| 下余白   | 本文末尾側の余白       | `20px`              |

> 上余白スライダーの表示上の既定値は `0px` ですが、内部的にはベース余白を含んだ値で管理しています。

### タイトル・著者表示

| 設定項目             | 説明                                         | 既定値 |
| -------------------- | -------------------------------------------- | ------ |
| タイトル・著者を表示 | タイトル・著者などの表示全体を切り替える     | `ON`   |
| 著者情報を表示       | `author` / `co_authors` を表示する           | `ON`   |
| 翻訳者情報を表示     | `translator` / `co_translators` を表示する   | `ON`   |
| 役割ラベルを表示     | `著` / `共著` / `訳` / `共訳` ラベルを出す   | `ON`   |
| 作品内ファイルのタイトル・著者も表示 | 作品内の本文ファイル固有の title / credits を表示する | `OFF` |
| タイトルを表示       | 作品内ファイル固有の title を表示する         | `ON`   |
| 著者を表示           | 作品内ファイル固有の authors を表示する       | `ON`   |

単独文書では Markdown frontmatter の title / author / translator を表示します。作品内の本文ファイルでは `.nyoze/books.json` v3 の Book 名・著者と、各ファイルの title / authors / translators を表示し、frontmatter へは fallback しません。

作品内の Book 先頭本文では Book 名・著者を表示できます。**作品内ファイルのタイトル・著者も表示**を ON にすると、先頭本文を含む各本文ファイル固有の title / credits も表示します。**タイトルを表示**と**著者を表示**で個別に切り替えられます。資料・未登録ファイルには作品内の冒頭情報を表示しません。

### UIテーマ

| 設定項目         | 説明                                                       | 既定値       |
| ---------------- | ---------------------------------------------------------- | ------------ |
| UI言語           | `ja` / `en` / `mixed` の表示モードを切り替える            | `mixed`      |
| 選択             | アプリ全体の配色テーマを切り替える                         | `Greige`     |
| UIフォント       | UI の書体を切り替える                                      | `ゴシック体` |
| UI文字色         | UI 文字色をテーマ色から個別に上書きできる                 | テーマ色     |
| UI文字サイズ倍率 | UI 全体の文字サイズ倍率                                    | `1.00x`      |
| テーマ管理       | UI テーマプリセットの作成・編集画面を開く                 | —            |

`mixed` は short label や tooltip を英語、helper や説明文を日本語へ寄せる stage1 モードです。現時点では主要 visible label を中心に適用しています。

UI テーマの選択メニューでは、テーマ名の右側に背景色とアクセント色の 2 色プレビューが表示されます。カスタムテーマは標準テーマとは別グループとして表示されます。

UI文字サイズ倍率を変更すると、左右ペインのタブや compact icon button も追従して拡大・縮小します。上部ツールバーは **ツールバーサイズ倍率** を独立して使います。Theme Studio の狭い表示では、スライダーやプルダウンなど複数部品を持つ設定行はラベルと操作部を2行に分け、色だけの設定行は1行表示を維持します。

### ツールバー

| 設定項目             | 説明                                         | 既定値   |
| -------------------- | -------------------------------------------- | -------- |
| ツールバーアイコン色 | ツールバーのアイコン色                       | 通常色   |
| ツールバー線の太さ   | アイコン線の太さ                             | `1.10`   |
| ツールバーサイズ倍率 | ツールバー全体のサイズ倍率                   | `1.00x`  |

### アプリロゴ

| 設定項目       | 説明                                               | 既定値           |
| -------------- | -------------------------------------------------- | ---------------- |
| アプリロゴ表示 | 上部ヘッダー内のアプリ名表示を切り替える           | `ON`             |
| アプリ名       | 表示名プリセットを選ぶ                             | `Nyoze`          |
| カスタム名     | `アプリ名` を `カスタム...` にしたときの任意名称   | `Nyoze`          |
| アプリ名文字色 | アプリ名の文字色                                   | テーマ色         |
| アプリ名フォント | アプリ名の書体                                   | `UIフォントと同じ` |

### Paragraph Plain

| 設定項目 | 説明 | 既定値 |
| -------- | ---- | ------ |
| Paragraph Plain の動作 | アプリ全体で、Paragraph Plain 中の入力・クリックなどの軽さと、後続本文の見え方のバランスを選びます。**軽さ優先**は操作を軽くしますが、編集中の段落が後続本文に一時的に重なることがあります。**表示優先**は後続本文を隠しにくい表示を優先します。スクロール中のオーバーレイ追従は抑えていますが、文書の内容によっては一部の操作が重くなることがあります。 | `軽さ優先` |

この項目は **文書には保存されず**、`settings.json` のアプリ設定として保持されます（frontmatter には書き込みません）。

### 文書テーマ

| 設定項目     | 説明                                               | 既定値                |
| ------------ | -------------------------------------------------- | --------------------- |
| 選択         | 本文領域の配色テーマ                               | `UIテーマに追従`      |
| ページ色     | 本文領域の背景色                                   | UIテーマ連動          |
| 本文色       | 本文文字色                                         | UIテーマ連動          |
| キャレット色 | 文字カーソル色。`自動（背景に合わせる）` / `ハイライト色`（UIテーマのアクセント色） / `カスタム` を選べる | `自動（背景に合わせる）` |
| エディタで矢印ポインターを使う | Windows の一部環境で I-beam ポインターが白く見えなくなる場合の回避策。通常編集・Paragraph Plain・Source Mode に適用 | `OFF` |
| 擬似キャレット | WYSIWYG 通常編集で、標準キャレット位置に表示専用の補助キャレットを重ねる。ON 時は WYSIWYG の標準キャレットを非表示にする。色はキャレット色設定に従う | `ON` |
| 擬似キャレットの太さ | 擬似キャレットの短軸の太さ。`1px`〜`8px` を `0.5px` 刻みで調整できる | `2px` |
| 擬似キャレットの点滅 | 擬似キャレットを単純な ON/OFF 切り替えで点滅させる。OS の reduced motion 設定が有効な場合は常時表示 | `ON` |

文書テーマの選択メニューでは、テーマ名の右側にページ色と本文色の 2 色プレビューが表示されます。`UIテーマに追従` は現在の UI テーマまたは UI プリセットの文書色に合わせたプレビューになります。

擬似キャレットは表示専用です。ProseMirror の selection、入力、Undo / Redo、IME composition、保存 Markdown は変更しません。`Source Mode` と `Paragraph Plain` ではそれぞれの入力面の native caret を使い、擬似キャレットは表示されません。

### サポート

| 設定項目           | 説明                                             | 既定値 |
| ------------------ | ------------------------------------------------ | ------ |
| 不具合を報告       | 不具合報告フォームを開く                         | —      |
| フィードバックを送る | 感想・要望フォームを開く                       | —      |
| 更新を確認         | GitHub Releases の公開版を確認する。主に GitHub zip 版など Store 版以外の更新確認向け | —      |
| リポジトリを開く   | ソースコードと GitHub 配布ページを既定ブラウザで開く。Store 版は Store アプリのライブラリから更新できる | —      |

表示設定ダイアログの各セクションは見出しクリックで折りたたみ/展開できます。見出し左側に開閉トグルとアイコンを表示し、**開くたびに全セクション折りたたみ**から始まります。開閉状態は保存されません。

`auto TCY` は縦書きの通常編集（WYSIWYG）、縦書きで開いた Page Viewer（現在の文書 / Book 全体）、および書き出し開始時点の Display Settings を snapshot として埋め込んだ Web Book（縦書き表示時）で有効です。`Source Mode` と `Paragraph Plain`、横書きの Page Viewer / Web Book では効かず、保存しても Markdown に `｟...｠` を自動追加しません。Page Viewer / Web Book では起動・書き出し時点の設定を snapshot として使い、開いたあとの Display Settings 変更は反映しません。英字を含む短い単語や URL の一部まで TCY 表示されるのを避けたい場合は、`数字だけを対象にする` を有効にしてください。

設定は `settings.json`（`app.getPath('userData')/settings.json`）に即時保存されます。localStorage にもフォールバックとしてミラーされます。
初回起動時に既存の localStorage 設定値が settings.json に自動移行されます。

### hidden 設定: macOS Arrow スクロール補正を native に戻す

通常は変更不要ですが、macOS 環境で Arrow キー移動時のスクロール補正を無効にして **ブラウザ / Electron の native 挙動へ戻したい**場合は、`settings.json` に次の hidden キーを追加または変更してください。

```json
{
  "macosArrowScrollClampEnabled": false
}
```

- 既定値は `true` です
- `false` にすると、通常編集（WYSIWYG）と Source Mode の両方で、この補正を無効にします
- frontmatter には保存されません
- 変更後は、いったん Nyoze を再起動して反映を確認してください

この設定は現時点では UI には表示していません。環境差の切り分けや、native 挙動へ一時的に戻したい場合のための保守用キーです。

### Document Type と改行解釈

通常 UI では raw な改行ポリシー名は表示しません。改行解釈は **Document Metadata** の **Document Type** から間接的に決まります。Document Type は「文書の性格・改行スタイル」を表す設定で、**表示方向（縦書き / 横書き）の設定ではありません**。表示方向は Document Metadata 下部の **表示方向** サマリと、上部ヘッダーの縦横切替で別管理します。

| Document Type（表示名）        | 内部値    | 説明                                                          |
| ------------------------------ | --------- | ----------------------------------------------------------- |
| `小説・本文`（en: Fiction）    | `novel`   | テキストエディタに近い操作感。Enter で改行                   |
| `記事・文書`（en: Article / Document） | `article` | Markdown エディタの操作感。Enter で段落区切り / Shift+Enter で段落内改行 |
| `未設定`                       | （なし）  | 標準の執筆設定を使います                                     |

Document Metadata で Document Type を保存すると、通常は frontmatter に `documentType: novel` または `documentType: article` が書かれます。`未設定` は Nyoze 管理の Document Type key なしが canonical です。内部値 `novel` / `article` と frontmatter key は不変で、今回変えたのは UI 表示名（`Novel` → `小説・本文` / `Fiction`、`Article` → `記事・文書` / `Article / Document`）だけです。

表示方向の分離は段階導入中です。`documentType: novel` / `article` / 未設定文書のそれぞれの既定表示方向は、**Display Settings の「文書タイプ別の既定表示方向」** で **縦書き / 横書き** から選べます。初期値は互換維持で、小説・本文＝縦書き、記事・文書＝横書き、未設定文書＝縦書きです。未設定文書も含めて、各文書タイプの既定方向を明示的に選びます。この設定は **frontmatter に `writingMode` が無い文書にだけ** 効きます。明示的なタブ単位の縦横切替や文書の `writingMode` がある場合はそちらを優先します。詳細な現行経路の棚卸しと方針は [`docs/document-type-writing-direction-audit-2026-06.md`](docs/document-type-writing-direction-audit-2026-06.md) を参照してください。

文書 frontmatter に `writingMode: vertical-rl`（縦書き）または `writingMode: horizontal-tb`（横書き）があると、その文書の実効表示方向は **Document Type 別の既定表示方向より優先** されます。空・複雑な値（リスト / ブロックスカラーなど）・未対応値は無視し、その場合は Document Type 別の既定表示方向に従います。タブ単位で縦横切替を明示操作したセッションでは、その操作が引き続き優先されます。

Document Metadata 下部の **表示方向** には、`現在の表示`（縦書き / 横書き）と `適用元`（**手動切替** / **文書の指定** / **文書タイプ別の既定**）が出ます。未対応の `writingMode` がある場合は「未対応の writingMode は無視されています」と表示します。上部ヘッダーの縦横切替でタブ単位の手動上書きをしている間は **手動切替を解除** が有効になり、押すと手動上書きを解除して、文書の `writingMode` または文書タイプ別の既定に戻します。

その下の **この文書の表示方向** プルダウンから、現在の文書の `writingMode` を明示的に保存 / 削除できます。

- **既定に従う**: frontmatter から `writingMode` key を削除し、文書タイプ別の既定表示方向へ戻します。未対応値が入っていた場合の掃除にも使えます。
- **縦書きに固定** / **横書きに固定**: frontmatter に `writingMode: vertical-rl` / `horizontal-tb` を書き込みます。文書タイプ別の既定より優先され、保存（未保存マーク）が必要です。

保存はこのプルダウン操作のときだけ行い、自動では `writingMode` を追加しません。本文（Markdown 本体）は変更しません。frontmatter が複雑で安全に書き換えられない文書、`Source Mode` / `Paragraph Plain` 編集中は、このプルダウンも無効になります。Document Metadata の保存対象キーは `documentType` / `nyozePreserveEmptyParagraphs` / `title` / `author` / `translator` に `writingMode` を加えたものです（`book` / `order` / `role` は対象外）。

`Article` では既定で `commonmark-strict` の canonical 動作を使います。連続空行を空段落として保持したい場合は、**Document Metadata** の **Paragraph Spacing → Preserve empty paragraphs** を ON にすると、frontmatter に `nyozePreserveEmptyParagraphs: true` が保存されます。OFF は key なしが canonical です。

既存の Article 文書に frontmatter key がなくても、source 上に今回の初期スコープで保持対象となる追加空行があれば、load 時に自動保護されます。これは runtime 上の effective 状態であり、frontmatter へは勝手に書き込みません。Document Metadata ではチェック済みで表示され、必要なら OFF で strict canonical へ戻すか、**Save as document setting** で explicit ON にできます。

既存文書に `nyozeType: novel` / `nyozeType: article` がある場合は読み取り互換として扱い、明示保存時には可能な範囲で `documentType` へ寄せます。`documentType` が配列・object・複数行 scalar・未定義の独自値などユーザー管理の値として使われている場合は、その値を保持したまま Nyoze 用 fallback として `nyozeType` を使います。`type` は scalar の `novel` / `article` / `note` だけを既存互換として読み、Document Metadata から新規書き込み・上書き・削除はしません。

> 文書内に旧互換用の `nyozeLineBreakPolicy` が残っている場合は、上部ヘッダーと Document Metadata に **`固定`** が表示されます。通常 UI では raw policy 名は出しません。互換設定を編集・削除したい場合は Source Mode を使ってください。

### UIテーマ

**View Settings** ダイアログ内の「UIテーマ」で切り替えます。アプリ全体の外観（ヘッダー、ペイン、ダイアログ等）に適用されます。
選択メニューには、各テーマの目安として背景色とアクセント色の 2 色プレビューが表示されます。

| テーマ        | 説明                         |
| ------------- | ---------------------------- |
| Greige        | 低コントラストの中間色テーマ |
| Taupe         | 暖色寄り中間色テーマ         |
| Linen         | 明るめの中間色テーマ         |
| Dove          | やわらかい灰色系テーマ       |
| Clay          | 土色寄りの中間色テーマ       |
| Olive         | 黄緑寄りの中間色テーマ       |
| Plum          | 華やかさのある中間色テーマ   |
| Light         | 標準ライトテーマ             |
| Sakura        | 桜色寄りの淡いライトテーマ   |
| Harbor        | くすんだ水色系ライトテーマ   |
| Sage          | くすんだ緑系ライトテーマ     |
| Dark          | 標準ダークテーマ             |
| Moss          | 深い緑系ダークテーマ         |
| Slate         | 青灰色系ダークテーマ         |
| Merlot        | ワインレッド系ダークテーマ   |
| Graphite      | 青紫寄りダークテーマ         |
| Midnight Blue | 深い青系ダークテーマ         |

### 文書表示テーマ

**View Settings** ダイアログ内の「文書表示テーマ」で切り替えます。エディタ本文領域のみに適用され、UIテーマとは独立して設定できます。
選択メニューには、各テーマの目安としてページ色と本文色の 2 色プレビューが表示されます。`UIテーマに追従` は現在の UI テーマまたは UI プリセットの本文色へ追従します。

| テーマ         | 説明                                                           |
| -------------- | -------------------------------------------------------------- |
| UIテーマに追従 | UIテーマ（またはUIプリセット）の配色を本文にも適用する（既定） |
| Paper Light    | 暖色系の紙風ライトテーマ（本文のみ）                           |
| Paper Dark     | 暖色系の紙風ダークテーマ（本文のみ）                           |
| BOW            | 白地に黒文字の基本テーマ                                       |
| WOB            | 黒地に白文字の基本テーマ                                       |
| Soft Neutral   | 中間色ベースのやさしい本文テーマ                               |

### 文書フォント

文書フォントプリセットで本文書体を切り替えられます。

| プリセット | 説明                                                      |
| ---------- | --------------------------------------------------------- |
| デフォルト | UIテーマのフォント設定をそのまま使用する                  |
| 明朝体     | ヒラギノ明朝・游明朝・Noto Serif JP 等のセリフ書体        |
| ゴシック体 | ヒラギノ角ゴ・游ゴシック・Noto Sans JP 等のサンセリフ書体 |

### フォント登録

フォントセクション内のテキスト入力欄にフォント名を入力して「登録」ボタンで登録フォント一覧に追加できます。
「システムフォント取得」ボタンを押すと OS にインストールされているフォント一覧を取得し、クリックで登録できます。

登録したフォントはラジオボタンで選択でき、選択中のフォントがエディタ本文に即時反映されます。
「なし（プリセットのみ）」を選択するとプリセットのみが適用されます。

登録フォントは `settings.json`（`app.getPath('userData')/settings.json`）に保存され、localStorage にもフォールバックとしてミラーされます。

### カスタムカラー

文書テーマセクション・見出し設定セクションでは、文書表示テーマに関係なくカラーを編集できます。
編集した色は本文表示に即時反映されます。

| 設定         | 説明                                                                                                                                                 | 既定値  |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| ページ色     | エディタ本文領域の背景色                                                                                                                             | #f5f1eb |
| 本文色       | 本文テキストの色                                                                                                                                     | #3a3530 |
| 見出し色     | 見出しテキストの色                                                                                                                                   | #2e2924 |
| キャレット色 | テキストカーソルの色。「自動（背景に合わせる）」ではページ色の輝度から高コントラストな色を自動選択し、「ハイライト色」では UI テーマのアクセント色を使い、「カスタム」では任意の16進カラーを指定できます | 自動    |

### 見出し設定

見出し設定セクションでは次を調整できます。

- 見出しフォント
- 見出し色
- 見出し位置（横書きでは左 / 中央 / 右、縦書きでは上 / 中央 / 下）
- 見出し後マージン（h1〜h6共通）
- 見出し区切り線（h1〜h6レベル別ON/OFF）

### ツールバーアイコン外観

ツールバーアイコン外観の設定は、ツールバー本体だけでなく左右ペインの開閉アイコン（サイドバー開閉ボタン）にも連動して適用されます。

- アイコン色: 通常状態のアイコン色を変更
- 線の太さ: Tablerアイコンの線幅を変更
- サイズ倍率: アイコンとボタンサイズを同時に拡大/縮小（トップバーに収まる範囲で自動クランプ）

hover時の背景表示、選択状態時の枠線+背景表示は、アイコン外観カスタマイズと同時に有効です。

### メニュー操作

| プラットフォーム | メニューアクセス                                                       |
| ---------------- | ---------------------------------------------------------------------- |
| macOS            | 画面上部のネイティブメニューバー（File / Edit / View / Window / Help） |
| Windows / Linux  | 上部ヘッダー左端のハンバーガーボタン（≡）からポップアップメニュー      |

メニューから実行できる主な操作:

| メニュー | 項目                                          | ショートカット   |
| -------- | --------------------------------------------- | ---------------- |
| File     | New Document                                  | Cmd/Ctrl+N       |
| File     | Open…                                         | Cmd/Ctrl+O       |
| File     | Save                                          | Cmd/Ctrl+S       |
| File     | Save As…                                      | Cmd/Ctrl+Shift+S |
| File     | バックアップフォルダを開く                    | —                |
| File     | このファイルのバックアップを開く              | —                |
| View     | View Settings…                                | —                |
| Edit     | Undo / Redo / Cut / Copy / Paste / Select All | 標準キー         |
| Help     | MANUAL を開く                                 | —                |
| Help     | ショートカットキー一覧                        | —                |
| Help     | 不具合を報告                                  | —                |
| Help     | フィードバックを送る                          | —                |

### 不具合報告とフィードバック

不具合報告と一般フィードバックは別フォームです。

**アクセス方法（どちらか一方でOK）**

1. **メニューから**: Help → 不具合を報告 / フィードバックを送る
2. **表示設定から**: ツールバーの **`View Settings`** を開き、**「サポート」** セクション内の該当ボタンをクリック

使い分け:

- **不具合を報告**: クラッシュ、保存失敗、表示崩れ、重さ、原稿破壊の疑いなど
- **フィードバックを送る**: 感想、改善要望、使い勝手、今後ほしい機能など

クリックすると既定のブラウザで各フォームが開きます。
URLを開けなかった場合は、その旨のダイアログが表示されます。

### MANUAL とショートカットキー一覧

Help メニューには、補助ドキュメントとして次の 2 つがあります。

- **MANUAL を開く**: GitHub 上の `MANUAL.md` を既定ブラウザで開きます
- **ショートカットキー一覧**: アプリ内の read-only internal doc を開きます

ショートカットキー一覧は通常文書ではないため、保存や frontmatter 編集はできません。

### サポート

表示設定ダイアログの **「サポート」** セクションには次の導線があります。

- 不具合を報告
- フィードバックを送る
- 更新を確認（GitHub Releases の公開版を確認。主に GitHub zip 版など Store 版以外の更新確認向けです。Store 版ではこの項目は表示されません）
- リポジトリを開く（ソースコードと GitHub 配布ページを開く。Store 版は Store アプリのライブラリから更新できます）

---

## フロントマター表示

Markdown ファイルの冒頭に YAML フロントマターが含まれている場合、エディタ領域の上部（縦書き時は右端）にタイトル群と著者/翻訳者群を読み取り専用で表示します。

表示設定の **「フロントマター表示」** セクションから、次を切り替えられます。

- フロントマター全体の表示/非表示
- 著者情報の表示/非表示
- 翻訳者情報の表示/非表示
- `著` / `共著` / `訳` / `共訳` ラベルの表示/非表示

### 対応フィールド

表示用フィールドの詳細は [`docs/frontmatter-reference.md`](docs/frontmatter-reference.md) を参照してください。

| フィールド       | 表示位置                                         | CSS クラス                    |
| ---------------- | ------------------------------------------------ | ----------------------------- |
| `title`          | block-start 側（横書き：左上、縦書き：列の上端） | `.frontmatter-title`          |
| `original_title` | `title` の下                                     | `.frontmatter-original-title` |
| `subtitle`       | `original_title` の下                            | `.frontmatter-subtitle`       |
| `author`         | block-end 側（横書き：右下、縦書き：列の下端）   | `.frontmatter-person`         |
| `co_authors`     | `author` の下                                    | `.frontmatter-person`         |
| `translator`     | `author` / `co_authors` の下                     | `.frontmatter-person`         |
| `co_translators` | `translator` の下                                | `.frontmatter-person`         |

### フォーマット

```yaml
---
title: 明暗
original_title: Meian
subtitle: めいあん
author: 夏目漱石
co_authors:
  - 夏目金之助
  - 夏目銀之助
  - 夏目馬之助
translator: John Doe
co_translators:
  - Jane Smith
  - John Smith
---
```

- `co_authors` / `co_translators` のように複数名を書く場合は、上の例のように `-` を使った YAML 配列で書くのが最も安全です。
- 次のような 1 行形式も読めますが、カンマを含む名前や将来の互換性を考えると、複数名は配列形式を推奨します。

```yaml
co_authors: [猫乃左手, 猫乃右手]
```

- 引用符付きの文字列も使えます。

```yaml
co_authors:
  - "Doe, John"
  - "Smith, Jane"
```

- フロントマターが存在しない場合、または上記フィールドがすべて空の場合は何も表示されません。
- フロントマターはエディタの SoT（ProseMirror ドキュメント）とは独立して保存され、保存 / 読み込み時も改変されません。
- beta ではフロントマターの一般編集 UI はありません。ただし右ペイン **Document Metadata** から `Document Type` / `title` / `author` / `translator` と、記事・文書（Article / Document）用の `nyozePreserveEmptyParagraphs` の限定編集だけができます。作品内の表示 metadata は **作品** タブ（books.json v3）が正本で、Document Metadata の frontmatter 編集とは同期しません。
- 作品内の Book 所属、章順、資料分類と表示 metadata は `.nyoze/books.json` **v3** が正本です。`book` / `order` / `role` frontmatter は正式な Project / Book 管理では使いません。

作品内の章ファイルや資料ファイルでは、作品タブ上の表示名は `.nyoze/books.json` v3 の `title` から決まります。本文中に見せるタイトルが必要な場合は Markdown 見出しを書くのが基本です。作品内ファイルの frontmatter 表示は既定で抑制され、単独文書向けの title / author 表示とは分けられています。

### 作品内本文の冒頭表示（books.json v3）

**Source of truth**

- **作品内**: Book 構造と表示 metadata（Book 名 / 著者、本文・資料の title / authors / translators）の正本は `.nyoze/books.json` **v3** です。frontmatter の `title` / `author` / `translator` へは fallback しません。
- **作品外の単独文書**: 冒頭表示の正本は従来どおり Markdown **frontmatter** です。

作品内の **本文（body）ファイル**を開いているときは、次のブロックを文書冒頭に読み取り専用で表示します。

1. **Book block**（Book 先頭本文のみ）: v3 の `books[].name` / `books[].authors`
2. **file block**（表示設定で ON のとき）: v3 の当該 item の `title` / `authors` / `translators`

Book 先頭本文では両方を併記できます。2 件目以降の本文は file block のみです。資料・未登録ファイル・作品外単独文書ではこれらの作品用 block は出ません（単独文書は frontmatter 表示）。

- 表示は **表示設定 > タイトル・著者表示** の master トグルが ON のときだけ出ます。
- 作品内本文では、単独文書向けの frontmatter 冒頭表示の代わりに v3 metadata を使います（二重には出ません）。
- **作品内ファイルのタイトル・著者も表示**（既定 OFF）を ON にすると file block が出ます。短編集・アンソロジーなど、章ごとに個別 title / credits がある場合に使います。
- Book title は file title より大きく表示し、両 block の間に十分な余白を取ります。
- 表示専用です。Markdown 本文 / frontmatter / `.nyoze/books.json` はこの表示だけでは書き換えません。v3 metadata の編集は右ペイン **作品** タブから行います。`Source Mode` / `Paragraph Plain` 中は非表示です（frontmatter 冒頭表示と同様）。

### 文書メタデータ（限定編集UI）

編集可能な key・作品内外の正本の違い・YAML 制限の正本は [`docs/frontmatter-reference.md`](docs/frontmatter-reference.md) です。以下は操作の要約です。

右ペインの **Document Metadata** タブ（内部識別子は `document`）では、現在の Markdown ファイルの **frontmatter** とファイル単位の表示設定だけを GUI で編集できます。

**Source of truth の整理**

- **作品外の単独文書**: 冒頭表示などで使う title / author / translator の正本は **frontmatter** です。Document Metadata の編集がそのまま表示 metadata になります。
- **作品内の登録済み本文・資料**: 作品タブや文書冒頭で使う表示 title / authors / translators の正本は `.nyoze/books.json` **v3** です。Document Metadata で frontmatter の `title` / `author` / `translator` を編集しても、**作品表示 metadata や冒頭表示には反映されません**（自動同期しません）。
- 作品内でも frontmatter 自体の編集は可能です。外部ツール連携や独自 metadata 用途向けに残ります。

作品内で Document Metadata を開くと、登録状態に応じて **作品タブへの導線** が表示されます。表示 metadata の編集は **作品** タブから行ってください。

パネル見出し横の `?` ボタンに、文書メタデータの対象範囲を説明するチップがあります。常時表示の長い説明文は置かず、作品内では登録状態に応じた短い案内だけを表示します。

編集できる項目:

- `Document Type`（`小説・本文` / `記事・文書` / `未設定`。英語 UI では `Fiction` / `Article / Document` / `Unset`）
- `Paragraph Spacing > Preserve empty paragraphs`（記事・文書 / Article のときだけ）
- `title`
- `author`
- `translator`
- 表示方向（縦書き / 横書き）の文書 frontmatter `writingMode` と、タブ単位の手動切替の解除

Document Type は通常 `documentType` に保存されます。`未設定` は Nyoze 管理 key なしが canonical です。既存文書に `documentType: note` / `nyozeType: note` / `type: note` があっても、UI 上は `未設定` として扱われます。

`Paragraph Spacing > Preserve empty paragraphs` は `Document Type = 記事・文書`（Article / Document）のときだけ表示されます。ON では `nyozePreserveEmptyParagraphs: true` が保存され、top-level の通常段落 / 見出し間の追加空行を空段落として保持します。単改行の意味は変わらず、list / blockquote / code block 周辺や leading / trailing blank lines の完全保持は beta の初期スコープ外です。

frontmatter key がない既存 Article 文書でも、source 内容から追加空行保持が必要と判定された場合は、自動保護として同じチェックが ON で表示されます。これは frontmatter の explicit ON とは別で、開いただけ / そのまま保存しただけでは空段落を落とさないための保護です。必要なら OFF で即時 canonical 化でき、明示的に保持設定を残したい場合は **Save as document setting** を使います。

`documentType` が配列・object・複数行 scalar・独自 scalar として使われている場合、Nyoze はその値をユーザー管理値として扱い、Document Metadata の保存では `documentType` を触らず `nyozeType` に fallback します。`type` は既存互換の scalar 読み取りだけで、保存時にも削除しません。

frontmatter が複雑で安全に書き換えられない場合、Document Metadata は read-only になり、Source Mode での編集を案内します。`Source Mode` / `Paragraph Plain` 編集中も read-only です。

入力を変更すると、通常の本文編集と同じく現在のタブが未保存状態になります。パネル下部の **保存** は、現在の Markdown 文書を通常の保存経路で保存します。変更がないときは無効です。保存に失敗した場合や保存をキャンセルした場合は未保存状態を維持します。作品内でこのボタンを使っても `.nyoze/books.json` は変更されません。

---

## タブ操作

Nyoze はタブストリップで複数文書を同時に管理できます。アクティブなタブのみがエディタに表示され、非アクティブなタブは Markdown スナップショットとして保持されます。

| 操作         | 動作                                                     |
| ------------ | -------------------------------------------------------- |
| `+` ボタン   | 新しい空のタブ（`untitled-N.md`）を追加して切り替える    |
| タブクリック | 対象タブに切り替える（現タブの内容はタブ内に保持される） |
| `×` ボタン   | タブを閉じる（未保存変更がある場合は確認ダイアログ表示） |

- 左ペインでファイルをクリックすると、**アクティブタブ**にそのファイルを読み込みます（タブは増えません）。未保存変更がある場合は確認ダイアログが表示されます。
- 左ペインのファイルを右クリック → 「Open in New Tab」で**新しいタブ**にファイルを開けます。
- 同時に開けるタブは最大 **12** です。上限に達すると `+` ボタンや「Open in New Tab」は無効化されます。メニューの「New Document」や `Shift + ファイルを開く` では通知が表示されます。
- 最後の 1 タブは閉じられません（`×` ボタンは非表示になります）。
- 既にファイルが開かれているタブと同じファイルを開くと、そのタブがアクティブ化されます（重複タブは作られません）。
- タブ切替時、Undo 履歴はリセットされます。

---

## ファイル操作

| ボタン         | 動作                                                                                                                                                         |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ファイルを開く`（旧 `Load`） | 単独ファイル（`.md` / `.markdown` / `.txt`）を開く。読み込み時はアクティブタブに文書を読み込み、左ペインを同一ディレクトリへ同期する。フォルダを書庫として使う場合は File メニューの「書庫を管理」から登録する |
| `Shift + クリック` | 新規タブで空の文書（`untitled-N.md`）を作成する                                                                                                              |
| `Save`         | 保存先パスが既知なら上書き保存。未知なら `Save As` にフォールバック                                                                                          |
| `Shift + Save` | 常に `Save As` で保存する                                                                                                                                    |

beta 版で正式にサポートする読み込み導線は、ツールバーの **ファイルを開く**（単独ファイル）と active 書庫内の File Explorer です。OS からの drag and drop、`Open With` からの起動引き渡し、`.md` の OS 全体関連付けは未対応です。

Nyoze はシングルインスタンスで動作します。すでに起動中の状態でもう一度 Nyoze を起動しようとすると、新しいウィンドウは開かず、既存のウィンドウが前面に移動します。

### 青空文庫風テキストの書き出し

通常の Markdown 保存とは別経路で、**現在アクティブな文書**を青空文庫に近いプレーンテキスト（`.txt`）へ書き出せます。

| 操作 | 動作 |
| ---- | ---- |
| **File > 書き出し > 青空文庫風テキストを書き出し...** | 保存ダイアログで出力先を選び、UTF-8 の `.txt` を書き出す |

- 書き出し元は、エディタ上の現在内容（PM Doc）です。未保存の変更があっても書き出せますが、**未保存状態は維持**され、元の Markdown ファイルは自動保存されません。
- 書き出しは **別ファイルへの export** です。開いているタブの保存先パス、File Explorer の表示、frontmatter、`.nyoze/books.json`、`.nyoze/notes.json` は変更しません。
- `Source Mode` または `Paragraph Plain` 中は実行できません。通常表示（WYSIWYG）に戻してから使ってください。
- built-in の読み取り専用 internal doc（ショートカット一覧など）では無効です。
- 対象は **アクティブ文書 1 件**だけです。同一 Book の複数章をまとめて書き出す場合は [Book 全体の書き出し](#book-全体の書き出し) を使ってください。
- 保存ダイアログの**前**に、下記の書き出しオプション確認 UI が開きます。キャンセルした場合は保存ダイアログを開かず、書き出しも行いません（通知も出しません）。

#### 書き出しオプション（確認 UI）

Book 全体の書き出しと同じ options 確認 UI を、この active document 単体の書き出しでも使います。

| option | 概要 | 既定 |
| ------ | ---- | ---- |
| 改ページを反映する | 明示 `:::page-break` と見出し前自動改ページ設定を書き出しへ反映。オフにするとこれらの改ページは出力されない（`:::blank-page` はこの設定に関係なく常に出力される） | ON |
| 見出しの前で自動改ページ | 各見出し直前に自動改ページ。「改ページを反映する」がオフのときは無効 | OFF |
| 対象見出しレベル | 「見出しの前で自動改ページ」の対象を `H1のみ` 〜 `H1〜H6` から選択。「見出しの前で自動改ページ」がオフのときは無効 | H1のみ |
| 文書情報を表示 | 現在の文書の frontmatter（`title` / `author` / `translator`）を本文冒頭に表示（[Web Bookを作成](#web-bookを作成) の「文書情報を表示」と同趣旨。青空文庫風では title は通常テキスト、著者・訳者は `［＃地付き］` 行） | OFF |
| 役割ラベルを表示 | 「文書情報を表示」がオンのときだけ有効。著者・訳者行に「著　」「訳　」ラベルを付けるか | ON |

「章の境界に改ページを入れる」は Book 全体の書き出し専用の option のため、この active document 単体の書き出しでは表示されません（章という単位が無いため）。「目次を表示」は Web Book 専用のため、この青空文庫風 TXT 書き出しでは表示されません。option は **書き出しオプション確認で確定した内容**を、書き出し形式（青空文庫風 / LeME / でんでん / Web Book）ごとに `settings.json` の `externalExportOptionsDefaults` へ保存し、次回以降の同形式の書き出し時に初期値として復元します（Cancel では保存しません。frontmatter / `.nyoze/books.json` には保存しません）。

#### 書き出される内容の概要

- 文字コードは **UTF-8** です。
- frontmatter は出力しません（本文ブロックのみ）。
- 付箋アンカー（`<!-- nyoze-note:... -->`）は出力しません。
- 変換は「青空文庫完全準拠」ではなく **青空文庫風** です。Nyoze の表示内容を、青空文庫でよく使う注記・ルビ表記へ近づけて `.txt` にします。

主な対応:

| Nyoze の内容 | 書き出しの扱い（概要） |
| ------------ | ---------------------- |
| ルビ | 青空形式 `｜親文字《ルビ》` |
| 縦中横（TCY） | `12［＃「12」は縦中横］` のような注記 |
| 太字 / 斜体 | `［＃太字］` / `［＃斜体］` とその終了注記 |
| 見出し h1 | `［＃「…」は大見出し］` |
| 見出し h2 / h3 | `［＃「…」は中見出し］` |
| 見出し h4〜h6 | `［＃「…」は小見出し］` |
| `:::align-center` | `５字下げ`への近似（`［＃ここから５字下げ］` … `［＃ここで字下げ終わり］`）。「ページの左右中央」注記は使いません（詳細は下記） |
| `:::align-end` | 内容が1行なら前置き型 `［＃地付き］本文`。複数行ならブロック指定 `［＃ここから地付き］` … `［＃ここで地付き終わり］`（行末への付与はしません） |
| `:::indent-1`〜`:::indent-6` | `［＃ここから１字下げ］`〜`［＃ここから６字下げ］`（**全角数字**）… `［＃ここで字下げ終わり］` |
| 水平線（`---`） | `＊　＊　＊` |
| 画像 | `［＃説明（ファイル名）入る］`（画像サイズは取得しません。詳細は下記） |
| 箇条書きリスト / 番号付きリスト | `・項目`、`1. 項目`（開始番号があれば反映。チェックリストは通常の箇条書きへ格下げ） |
| 引用（blockquote） | `［＃ここから１字下げ］` … `［＃ここで字下げ終わり］` で本文を近似（ネストしても本文順は保たれます。詳細は下記） |
| `:::page-break`（改ページ marker） | AozoraEpub3 標準の改ページ注記 `［＃改ページ］`（意味が一致するため warning なし） |
| `:::blank-page` / `:::blank-page-N`（空白ページ marker） | `［＃改ページ］`（count+1 個）と全角スペースの filler（count 個）で近似（装飾の近似・省略と同じ通知が表示される。実機での見え方は未確認） |

#### AozoraEpub3 互換の字下げ注記

字下げ注記（`:::indent-N` / 引用の入れ子）は、実際に AozoraEpub3 で EPUB 変換した結果を踏まえて次のように出力します。

- **字下げの数字は全角数字**（`１`〜`６`）で出力します。半角数字は AozoraEpub3 側で `注記未変換` として扱われるため使いません。
- 入れ子になった引用や、引用と `:::indent-N` を組み合わせた場合は、**現在の位置からの相対値ではなく、行端からの絶対字下げ量**として出力します。例えば二重引用は `２字下げ`、三重引用は `３字下げ`になります。`:::indent-3` の中の引用は `４字下げ`（3 + 1）になります。
- 連続する字下げでは、途中で `［＃ここで字下げ終わり］` を重複させません。字下げが完全に終わる（字下げ量 0 に戻る）タイミングでだけ、1回出力します。

例（引用の三重ネスト）:

```
> これは引用ブロックです。
> 複数行の引用
> > 二重
> > > 三重
```

書き出し結果:

```
［＃ここから１字下げ］
これは引用ブロックです。
複数行の引用
［＃ここから２字下げ］
二重
［＃ここから３字下げ］
三重
［＃ここで字下げ終わり］
```

#### `:::align-center`（中央寄せ）を「ページの左右中央」注記にしない理由

青空文庫の `［＃ページの左右中央］` は、章扉や献辞などページ単位で文字を配置するための注記であり、本文中の一部だけを中央寄せにする Nyoze の `:::align-center` とは意味が異なります。誤って使うと、AozoraEpub3 側でページ全体のレイアウトとして解釈され、意図と異なる見た目になる可能性があります。

そのため、青空文庫風 txt 書き出しでは `:::align-center` を `５字下げ`（`［＃ここから５字下げ］` … `［＃ここで字下げ終わり］`）へ近似します。既存の字下げ（`:::indent-N` や引用の中）にある場合は、その字下げレベルに `5` を加算した絶対レベルになります。`unsupported-node` ではなく `approximate-directive` warning を出し、近似変換であることを示します。

`［＃ページの左右中央］` 注記自体は、将来 章扉・献辞などページ単位の directive を追加する際の候補として残していますが、現行の `:::align-center` には使いません。

#### 制限と注意

- **青空文庫の全文法・全注記への完全準拠は目指していません。** 上記は代表例であり、細部は将来の更新で変わる場合があります。
- `:::style-<id>`（手紙風 letter / 控えめ muted / 章題風 heading など）は、**本文テキストだけ**を出力します。青空固有の style 注記には変換しません（内部では warning として扱います）。
- 画像は `［＃説明（ファイル名）入る］` へ近似します。説明は alt を優先し、alt が空なら title、どちらも空なら「画像」を使います。ファイル名は `src` から取り出すだけで、**画像ファイルの読み込みやサイズ取得は行いません**。内部表示用の `nyoze-img://` URL は出力しません。
- コードブロック、表（GFM table 風のテキスト）、HTML ブロック / インライン HTML などは、構造をそのまま保持しません。テキストだけ残す、または省略し、必要に応じて warning となります。
- 取り消し線、リンク、ルビ以外の特殊装飾など、未対応の mark / node は落とすかテキストのみにします。
- **改ページ（`:::page-break`）は AozoraEpub3 標準の改ページ注記 `［＃改ページ］` として書き出されます。** 「ページの左右中央」や「地付き」のような意味のズレがある近似とは違い、改ページという意味そのものが一致するため warning は出ません。
- 書き出し成功時の通知は、改行ポリシー通知と同じ控えめなトーストです。変換時の warning 一覧を UI で詳しく見る機能はありません。

### LeME 互換 Markdown の書き出し

通常の Markdown 保存とは別経路で、**現在アクティブな文書**を LeME に渡しやすい Markdown（`.md`）へ書き出せます。

| 操作 | 動作 |
| ---- | ---- |
| **File > 書き出し > LeME 互換 Markdown を書き出し...** | 保存ダイアログで出力先を選び、UTF-8 の `.md` を書き出す |

- 実機確認の結果、LeME の `.md` 入力は基本 Markdown（見出し・太字・斜体・太字斜体）に加え、一部 HTML（`<ruby>` によるルビ、`style` 属性付き `<div>` / 見出しタグによる配置）が広く機能することが分かりました。そのため Nyoze の LeME 書き出しは **`.md` + HTML 併用**を標準にしています。
  - 以前 `.txt`（LeME Text(Nor)）向け出力を検討・実装しましたが、この実機確認結果により標準を `.md + HTML` へ戻しました。
- 書き出し元は、エディタ上の現在内容（PM Doc）です。未保存の変更があっても書き出せますが、**未保存状態は維持**され、元の Markdown ファイルは自動保存されません。
- 書き出しは **別ファイルへの export** です。開いているタブの保存先パス、File Explorer の表示、frontmatter、`.nyoze/books.json`、`.nyoze/notes.json` は変更しません。
- `Source Mode` または `Paragraph Plain` 中は実行できません。通常表示（WYSIWYG）に戻してから使ってください。
- built-in の読み取り専用 internal doc（ショートカット一覧など）では無効です。
- 対象は **アクティブ文書 1 件**だけです。同一 Book の複数章をまとめて書き出す場合は [Book 全体の書き出し](#book-全体の書き出し) を使ってください。
- 既定の保存名は、元ファイル名に `-leme` を付けた `.md` です。例: `chapter.md` / `chapter.markdown` / `chapter.txt` → `chapter-leme.md`。未保存タブでは `nyoze-leme-export.md` になります。
- 保存ダイアログの**前**に、下記の書き出しオプション確認 UI が開きます。キャンセルした場合は保存ダイアログを開かず、書き出しも行いません（通知も出しません）。

#### 書き出しオプション（確認 UI）

Book 全体の書き出しと同じ options 確認 UI を、この active document 単体の書き出しでも使います。

| option | 概要 | 既定 |
| ------ | ---- | ---- |
| 改ページを反映する | 明示 `:::page-break` と見出し前自動改ページ設定を書き出しへ反映。オフにするとこれらの改ページは出力されない（`:::blank-page` はこの設定に関係なく常に出力される） | ON |
| 見出しの前で自動改ページ | 各見出し直前に自動改ページ。「改ページを反映する」がオフのときは無効 | OFF |
| 対象見出しレベル | 「見出しの前で自動改ページ」の対象を `H1のみ` 〜 `H1〜H6` から選択。「見出しの前で自動改ページ」がオフのときは無効 | H1のみ |
| 自動 TCY を反映する | 短い英数字は `^...^`、`!!` / `!?` / `??` は LeME 既定 CSS の `<span class="tcy">...</span>` として出力（CSS ファイル自体は出力しない）。明示 TCY・ルビ・リンク・コードは対象外。Display Settings の自動 TCY とは独立（書字方向でも制限しない） | OFF |
| 数字だけを対象にする | 自動 TCY の対象を数字と `!!` / `!?` / `??` に絞り、英字混じりは除外。「自動 TCY を反映する」がオフのときは無効 | OFF |
| 最小桁数 / 最大桁数 | 自動 TCY の対象桁数（1〜4）。大小が逆転している場合は書き出し時に入れ替わる。「自動 TCY を反映する」がオフのときは無効 | 2 / 4 |
| 文書情報を表示 | 現在の文書の frontmatter（`title` / `author` / `translator`）を本文冒頭に表示（[Web Bookを作成](#web-bookを作成) の「文書情報を表示」と同趣旨。LeME では `# title` + 地付き credit div） | OFF |
| 役割ラベルを表示 | 「文書情報を表示」がオンのときだけ有効。著者・訳者行に「著　」「訳　」ラベルを付けるか | ON |

「章の境界に改ページを入れる」は Book 全体の書き出し専用の option のため、この active document 単体の書き出しでは表示されません（章という単位が無いため）。「目次を表示」は Web Book 専用のため、この LeME 互換 Markdown 書き出しでは表示されません。自動 TCY 系 option は LeME / でんでん向け書き出しだけで表示され、Web Book / 青空文庫風 TXT には出ません。option は **書き出しオプション確認で確定した内容**を、書き出し形式（青空文庫風 / LeME / でんでん / Web Book）ごとに `settings.json` の `externalExportOptionsDefaults` へ保存し、次回以降の同形式の書き出し時に初期値として復元します（Cancel では保存しません。frontmatter / `.nyoze/books.json` には保存しません）。

#### 書き出される内容の概要

LeME 互換 Markdown export は、LeME が解釈しやすい Markdown + HTML の組み合わせへ寄せるための出力です。LeME 側の設定や EPUB リーダーによって最終表示が変わる場合があるため、完全な表示一致を保証するものではありません。

主な対応:

| Nyoze の内容 | 書き出しの扱い（概要） |
| ------------ | ---------------------- |
| ルビ | HTML `<ruby>親文字<rt>ルビ</rt></ruby>` |
| 明示縦中横（TCY） | `^文字^` |
| 改行（Shift+Enter 相当） | `<br />`（単改行が省略されないように明示変換。ただし GFM table 風テキストは改行のまま） |
| 太字 / 斜体 / 太字 + 斜体 | `**太字**` / `*斜体*` / `***太字斜体***` |
| 打ち消し線 | `~~取消~~` |
| インラインコード | `` `code` ``（本文中に backtick があっても壊れないよう fence の長さを自動調整） |
| ハイライト | HTML `<mark>ハイライト</mark>` |
| アンダーライン（下線） | HTML `<u>下線</u>` |
| 見出し h1〜h6 | `#`〜`######` |
| 水平線 | `---` |
| 引用 | `>` を付けた Markdown 引用（引用内の太字 / ルビなども変換される） |
| 番号なしリスト | `- 項目` |
| 番号付きリスト | `1. 項目` |
| コードブロック | fenced code block（`` ``` `` + language。本文の backtick 衝突を自動回避） |
| 画像 | `![alt](src)` / `![alt](src "title")` の Markdown image syntax（保存されている src をそのまま使用） |
| 空の段落 | `<p><br /></p>`（空行が省略されないように変換） |
| `:::align-center` | 通常段落は `<div style="text-align:center;" markdown="1">...</div>`、見出しは `<h1 style="text-align:center;">...</h1>` のように HTML heading へ変換 |
| `:::align-end` | 通常段落は `<div style="text-align:right;" markdown="1">...</div>`、見出しは同様に HTML heading へ変換 |
| `:::indent-1`〜`:::indent-6` | `<div style="padding-top:1em;" markdown="1">...</div>` 〜 `padding-top:6em` |
| `:::page-break` | 独立した空 `<div style="page-break-before:always;"></div>`（旧 LeME 記法 `!PB` は使わない） |
| `:::blank-page` / `:::blank-page-N` | count 個の `page-break-before` / `page-break-after` 両持ち空 `<div>` で近似（装飾の近似・省略と同じ通知が表示される。最終的な見え方は EPUB reader 側の解釈にも依存） |

#### 制限と注意

- **タスクリスト**: `- [ ] 未完了` / `- [x] 完了` のようなタスクリストは、LeME が変換エラーになることを確認しています。そのため Nyoze は書き出し時に `[ ]` / `[x]` を出さず、**通常の番号なしリスト項目へ変換**します（`- [ ] タスク` → `- タスク`）。この変換は内部で警告として記録されます。
- **見出しと配置の両立**: `:::align-center` / `:::align-end` の内側に見出しがある場合、`markdown="1"` の `<div>` の中で `# 見出し` と書いても LeME 側では `#` がそのまま文字として表示され、見出しとして解釈されませんでした。そのため Nyoze は見出しを `<h1 style="text-align:center;">...</h1>` のような HTML heading タグへ変換し、見出し構造と配置の両方を保つようにしています（`#` を残したまま div で包む、または配置を省略することはしません）。
- **字下げは縦書き前提**: `:::indent-N` は `padding-top:Nem` の inline style で表現しており、縦書き文書を前提としています。横書き文書向けの `padding-left` への切り替えは未対応です。
- LeME 側の Markdown-in-HTML 解釈を安定させるため、書き出し結果の配置・字下げ wrapper 内には空行が入る場合があります。
- `:::style-<id>`（手紙風 letter / 控えめ muted / 章題風 heading など）は、**本文テキストだけ**を出力します。LeME 固有の style へは変換しません（内部では warning として扱います）。
- **リンクは本文のみになります。** LeME 実機でリンクが期待どおり表示されなかったため、URL は出力せず、リンクテキストだけを残します（内部では警告として記録されます）。
- **画像は Markdown image syntax（`![alt](src)`）として保持されます。** LeME `.md` はローカル画像を表示できることを実機で確認しています。ただし Nyoze は書き出し時に画像ファイルのコピーや、書き出し先に合わせた path の付け替え（rebasing）は行いません。保存されている `src` の文字列をそのまま Markdown image destination として書き出すだけです。ローカル画像を LeME 側から実際に読み込めるかどうかは、書き出し先ファイルと画像ファイルの相対位置をご自身で管理していただく前提です。Nyoze 編集画面の内部表示専用 URL（`nyoze-img://`）が書き出し結果に含まれることはありません。
- **表は、今回はまだ LeME 書き出しで構造を保持しません。** Nyoze 内での表編集自体が別スライスとなるため、今回の対象外です。省略されるか、テキストだけが残ります。ただし `| A | B |` のような GFM table 風のテキストを改行で区切って書いている場合は、LeME 側で表として解釈されやすいよう改行をそのまま保って書き出します（Nyoze 内で表として編集・表示できるようになるわけではありません）。
- **中央寄せ / 右寄せブロックの中に GFM table 風のテキストがある場合、その部分だけ配置よりも表としての表示を優先します。** `markdown="1"` の配置 wrapper の内側に表を入れると LeME 側で EPUB 変換エラーになることを確認したためです。この場合、表部分の中央寄せ / 右寄せは失われ、通常のブロックの外に出た形で書き出されます。同じブロック内の見出しや通常の段落の配置には影響しません。
- ハイライトと太字 / 斜体 / 打ち消し線を組み合わせた場合も、装飾が正しく入れ子になって出力されます（例: `*==斜体+ハイライト==*` → `*<mark>斜体+ハイライト</mark>*`）。
- **アンダーライン（下線）** は Nyoze 独自記法 `||text||` で入力・保存し、HTML `<u>下線</u>` で書き出します（warning なし）。太字 / 斜体 / 打ち消し線 / ハイライトとの組み合わせも正しく入れ子になって出力されます。
- 数式、脚注は LeME 側が非対応のため未対応です。
- HTML ブロック / インライン HTML（ユーザーが入力した raw HTML）は構造をそのまま保持しません。テキストだけ残す、または省略し、必要に応じて warning となります（ただし、書き出し処理自体が生成する `<ruby>` / `<mark>` / `<h1>` / `style` 付き `<div>` / `<br />` / `<p><br /></p>` は意図した出力であり、除去対象ではありません）。
- **改ページ（`:::page-break`）は独立した空 `<div style="page-break-before:always;"></div>` として書き出されます。** warning は出ません。旧 LeME Text(Nor) 記法の `!PB` は使わず、`:::align-center` / `:::indent-N` と同じ「inline style 付き `<div>` がそのまま通る」仕組みを使っています。ただし、この CSS プロパティ自体の実機表示確認はまだ行っていません。
- 章単位の個別ファイル分割、LeME 側のユーザー CSS / 設定ファイルの自動生成は未対応です。書き出しオプション確認 UI の選択内容は `settings.json` の `externalExportOptionsDefaults` に形式別・scope別で保存されます。自動 TCY は上記 option で有効化でき、英数字は `^...^`、記号3種（`!!` / `!?` / `??`）は LeME 既定の `<span class="tcy">` として出力します。Book 全体の一括書き出しと改ページ option は [Book 全体の書き出し](#book-全体の書き出し) を参照してください。
- 書き出し成功時の通知は、改行ポリシー通知と同じ控えめなトーストです。変換時の warning 一覧を UI で詳しく見る機能はありません。

### でんでんコンバーター向け Markdown の書き出し

通常の Markdown 保存とは別経路で、**現在アクティブな文書**をでんでんコンバーターへ渡しやすい Markdown（`.md`）へ書き出せます。

| 操作 | 動作 |
| ---- | ---- |
| **File > 書き出し > でんでんコンバーター向け Markdown を書き出し...** | 保存ダイアログで出力先を選び、UTF-8 の `.md` を書き出す |

- 書き出し元は、エディタ上の現在内容（PM Doc）です。未保存の変更があっても書き出せますが、**未保存状態は維持**され、元の Markdown ファイルは自動保存されません。
- 書き出しは **別ファイルへの export** です。開いているタブの保存先パス、File Explorer の表示、frontmatter、`.nyoze/books.json`、`.nyoze/notes.json` は変更しません。
- `Source Mode` または `Paragraph Plain` 中は実行できません。通常表示（WYSIWYG）に戻してから使ってください。
- built-in の読み取り専用 internal doc（ショートカット一覧など）では無効です。
- 対象は **アクティブ文書 1 件**だけです。同一 Book の複数章をまとめて書き出す場合は [Book 全体の書き出し](#book-全体の書き出し) を使ってください。
- 既定の保存名は、元ファイル名に `-denden` を付けた `.md` です。例: `chapter.md` / `chapter.markdown` / `chapter.txt` → `chapter-denden.md`。未保存タブでは `nyoze-denden-export.md` になります。
- 保存ダイアログの**前**に、下記の書き出しオプション確認 UI が開きます。キャンセルした場合は保存ダイアログを開かず、書き出しも行いません（通知も出しません）。

#### 書き出しオプション（確認 UI）

Book 全体の書き出しと同じ options 確認 UI を、この active document 単体の書き出しでも使います。

| option | 概要 | 既定 |
| ------ | ---- | ---- |
| 改ページを反映する | 明示 `:::page-break` と見出し前自動改ページ設定を書き出しへ反映。オフにするとこれらの改ページは出力されない（`:::blank-page` はこの設定に関係なく常に出力される） | ON |
| 見出しの前で自動改ページ | 各見出し直前に自動改ページ。「改ページを反映する」がオフのときは無効 | OFF |
| 対象見出しレベル | 「見出しの前で自動改ページ」の対象を `H1のみ` 〜 `H1〜H6` から選択。「見出しの前で自動改ページ」がオフのときは無効 | H1のみ |
| 自動 TCY を反映する | 短い英数字と `!!` / `!?` / `??` をでんでん向けの縦中横記法（`^...^`）として出力。明示 TCY・ルビ・リンク・コードは対象外。Display Settings の自動 TCY とは独立（書字方向でも制限しない）。LeME 向けとは記号3種の出力方式が異なる | OFF |
| 数字だけを対象にする | 自動 TCY の対象を数字と `!!` / `!?` / `??` に絞り、英字混じりは除外。「自動 TCY を反映する」がオフのときは無効 | OFF |
| 最小桁数 / 最大桁数 | 自動 TCY の対象桁数（1〜4）。大小が逆転している場合は書き出し時に入れ替わる。「自動 TCY を反映する」がオフのときは無効 | 2 / 4 |
| 文書情報を表示 | 現在の文書の frontmatter（`title` / `author` / `translator`）を本文冒頭に表示（[Web Bookを作成](#web-bookを作成) の「文書情報を表示」と同趣旨。でんでんでは `# title` + 地付き credit helper div） | OFF |
| 役割ラベルを表示 | 「文書情報を表示」がオンのときだけ有効。著者・訳者行に「著　」「訳　」ラベルを付けるか | ON |

「章の境界に改ページを入れる」は Book 全体の書き出し専用の option のため、この active document 単体の書き出しでは表示されません（章という単位が無いため）。「目次を表示」は Web Book 専用のため、この でんでん向け Markdown 書き出しでは表示されません。自動 TCY 系 option は LeME / でんでん向け書き出しだけで表示され、Web Book / 青空文庫風 TXT には出ません。option は **書き出しオプション確認で確定した内容**を、書き出し形式（青空文庫風 / LeME / でんでん / Web Book）ごとに `settings.json` の `externalExportOptionsDefaults` へ保存し、次回以降の同形式の書き出し時に初期値として復元します（Cancel では保存しません。frontmatter / `.nyoze/books.json` には保存しません）。

#### 書き出される内容の概要

でんでんコンバーター向け Markdown export は、でんでんコンバーターが解釈しやすい記法へ寄せるための出力です。でんでん側の設定や EPUB リーダーによって最終表示が変わる場合があるため、完全な表示一致を保証するものではありません。

主な対応:

| Nyoze の内容 | 書き出しの扱い（概要） |
| ------------ | ---------------------- |
| 段落 | top-level の段落は空行区切り（`\n\n`）で出力 |
| 空段落（空行） | `<p><br /></p>`（でんでん側で連続空行が畳まれて見た目の空きが消えるのを防ぐ） |
| 段落内改行（hardBreak） | 空行なしの改行のまま（でんでん側で `<br>` 相当） |
| ルビ | でんでん形式 `{親文字\|ルビ}` |
| 明示縦中横（TCY） | `^文字^` |
| 太字 | Markdown の `**太字**` |
| 斜体 | HTML `<i>斜体</i>`（実機確認済み。warning なし） |
| 太字 + 斜体 | `<i>**太字+斜体**</i>`（italic が外側。実機確認済み。warning なし。`***…***` は出力しない） |
| 打ち消し線 | HTML `<s>取り消し線</s>`（実機確認済み。warning なし） |
| ハイライト | HTML `<mark>ハイライト</mark>`（warning なし） |
| アンダーライン（下線） | HTML `<u>下線</u>`（warning なし） |
| インラインコード | Markdown backtick `` `code` ``（本文中の最長 backtick run より長い fence。warning なし） |
| リンク | Markdown link `[text](dest)` / `[text](dest "title")` をそのまま保持（実機確認済み。warning なし） |
| 見出し h1〜h6 | Markdown 見出し `#`〜`######` |
| 水平線 | `---` |
| 引用（blockquote） | 各行に `> ` を付けた Markdown 引用（内部の bold / italic / strike / highlight / link / code すべて維持） |
| コードブロック | fenced code block（language 維持） |
| 箇条書き / 番号付きリスト | Markdown リスト（`- item` / `N. item`）。チェックリストは通常の箇条書きへ降格（warning） |
| 画像 | Markdown image syntax `![alt](src)` / `![alt](src "title")` をそのまま保持（ファイルコピーや path 変換はしない） |
| `:::align-center` | `<div class="text-center" markdown="1">…</div>`（helper div 近似。見出しにも使える） |
| `:::align-end` | `<div class="text-right" markdown="1">…</div>`（helper div 近似。見出しにも使える） |
| `:::indent-1`〜`:::indent-6` | `<div style="padding-top:Nem;" markdown="1">…</div>` |
| `:::page-break`（改ページ marker） | 独立した空 `<div style="page-break-before:always;"></div>` |
| `:::blank-page` / `:::blank-page-N`（空白ページ marker） | LeME と同じ、count 個の `page-break-before` / `page-break-after` 両持ち空 `<div>` で近似（装飾の近似・省略と同じ通知が表示される。最終的な見え方は EPUB reader 側の解釈にも依存） |

#### 制限と注意

- top-level の段落は空行区切り（`\n\n`）で出力します。Nyoze の空段落（意図的な空行）は、でんでん側で連続空行が畳まれて見た目の空きが消えないよう `<p><br /></p>` へ変換します（warning は出しません。末尾の空段落は出力しません）。
- ルビは原則 `{親文字|ルビ}` 形式ですが、親文字またはルビに `{` / `}` / `|` を含む場合は、記法が壊れないよう **青空文庫風ルビ（`｜親文字《ルビ》`）へ自動的に切り替えます**（内部では warning として扱います）。壊れた記法や生の HTML は出力しません。
- **斜体**は、実機確認（2026-07-02）により HTML `<i>斜体</i>` で表示できることが判明したため、この形式で書き出します（warning なし）。**旧方針（圏点 / 傍点になるため本文テキストのみ + warning）は撤回しました。**
- **太字 + 斜体**も実機確認済みの `<i>**太字+斜体**</i>`（italic が外側）で書き出します（warning なし）。`***…***` は出力しません。bold の中に italic が一部だけ含まれる場合（例: `**太字の中に*斜体*を含む**`）も、bold の `**…**` は段落内で壊れず、italic だけが `<i>…</i>` として局所的に開閉されます。**旧方針（太字へ丸め + warning）は撤回しました。**
- **打ち消し線**は実機確認済みの HTML `<s>取り消し線</s>` で書き出します（warning なし。Markdown の `~~…~~` は出力しません）。
- **ハイライト**は HTML `<mark>ハイライト</mark>` で書き出します（warning なし）。
- **アンダーライン（下線）** は Nyoze 独自記法 `||text||` で入力・保存し、HTML `<u>下線</u>` で書き出します（warning なし）。
- **インラインコード**は Markdown backtick をそのまま維持します（warning なし。本文中に backtick run があっても、それより長い fence を自動で選びます）。
- **リンク**は Markdown link `[text](dest)` / `[text](dest "title")` としてそのまま保持します（実機確認済み。warning なし）。URL / title / link text の escape は通常 Markdown 保存経路と同じ helper を使います。link text の中に bold / italic / ruby / TCY などの inline 装飾が続けて入っている場合も、mark の境界が保たれていれば保持されます。リンク先の有効性確認や外部ファイルのコピーはしません。
- `<i>` / `<mark>` / `<s>` のような HTML tag の中に入る plain text は、`&` / `<` / `>` を `&amp;` / `&lt;` / `&gt;` へ escape します。
- `:::align-center` / `:::align-end` は、でんでん向けの `text-center` / `text-right` helper div による **近似**です。見出しを含む場合も helper div の中に Markdown 見出しとして残り、でんでん実機で中央寄せ / 行末寄せの見出しとして機能します。Nyoze の縦書き「地付き」と完全に同義ではなく、最終的な見た目はでんでん側の CSS と EPUB リーダーに依存します。
- `:::indent-1`〜`:::indent-6` は、LeME export と同じく `<div style="padding-top:Nem;" markdown="1">…</div>`（縦書き前提。横書き向け `padding-left` 切替は未対応）で字下げを表現します（warning なし）。段落先頭の全角スペースは折り返し行に効かず block indent として破綻するため採用しません。
- `:::style-<id>`（手紙風 letter / 控えめ muted / 章題風 heading など）は、**本文テキストだけ**を出力します（内部では warning として扱います）。
- 箇条書き / 番号付きリストは Markdown リストとして保持します。チェックリスト項目（`checked` 属性あり）は `[ ]` / `[x]` を出さず**通常の箇条書き項目へ降格**します（内部では warning として扱います）。
- 画像は保存されている `src` をそのまま Markdown image syntax として保持します。画像ファイルのコピーや export 先に合わせた path の rebasing はしないため、でんでん側から画像を読めるかは export 先と画像ファイルの相対位置に依存します。`nyoze-img://` のような Nyoze 内部表示用 URL は出力しません。
- 段落内改行（hardBreak）は物理改行のまま出力するため、GFM 表風テキスト（`| A | B |` など、Nyoze には表専用の node がなく段落 + 改行として保持される）もそのまま通ります。これは export 時の物理改行の副産物であり、Nyoze の編集・表示画面で表として扱われるわけではありません。
- 上付き文字 / 下付き文字は、Nyoze エディタ側にまだ対応する mark 自体がないため、今回は対象外です。下線は `||text||` で入力・保存し、HTML `<u>下線</u>` として書き出します。
- **改ページ（`:::page-break`）は独立した空 `<div style="page-break-before:always;"></div>` として書き出されます。** warning は出ません。でんでんの公式ヘルパー class（`text-center` / `text-right` 等）に改ページ専用のものは確認できなかったため、LeME 互換書き出しと同じ inline style 付き `<div>` を使っています。ただし、この CSS プロパティ自体の実機表示確認はまだ行っていません。
- でんでん設定ファイルの書き出しは未対応です。書き出しオプション確認 UI の選択内容は `settings.json` の `externalExportOptionsDefaults` に形式別・scope別で保存されます。自動 TCY（短い英数字と記号3種の `^...^`）は上記 option で有効化できます。Book 全体の一括書き出しと改ページ option は [Book 全体の書き出し](#book-全体の書き出し) を参照してください。
- 書き出し成功時の通知は、改行ポリシー通知と同じ控えめなトーストです。変換時の warning 一覧を UI で詳しく見る機能はありません。

### Web Bookを作成

**Web Book** は、現在の文書または Book 全体を、ブラウザで一冊として読む・共有する・簡易印刷/PDF保存するための HTML 出力です。ユーザー向けの HTML 出力はこの経路のみです。

- **ページめくり操作**: 生成された HTML を開くと、画面を左・中央・右の3分割として tap / click / swipe でページをめくれます。横書きは左を tap/click すると前のページ、右を tap/click すると次のページ、スマートフォン等では左へ swipe すると次のページ、右へ swipe すると前のページに切り替わります。縦書き（vertical-rl）は左右キー（`ArrowLeft` = 次のページ）と同じ向きで、左を tap/click すると次のページ、右を tap/click すると前のページ、右へ swipe すると次のページ、左へ swipe すると前のページになります。画面中央を tap/click すると、上部のツールバー（chrome）の表示・非表示だけを切り替えます（ページは動きません）。本文中のリンクやツールバー・Settings・目次のボタンをタップ/クリックしたときや、文字を選択しているときはページめくりは働きません。マウス操作では、これらの左右クリック・swipe に加えて、既存の前/次ボタンや矢印キーもそのまま使えます。
- **モバイルでの用紙枠**: 画面の短辺（スマートフォン判定用に使う、縦持ち・横持ちどちらでも短いほうの辺）が狭いタッチ端末で開いた場合、Settings の「用紙枠」は既定で OFF になり、外側余白を使わず画面いっぱいに本文を表示します。短辺で判定するため、横持ちにしたスマートフォンでも既定 OFF のままです（タブレットは短辺が広いため既定 ON のまま。デスクトップでウィンドウを狭くしただけの場合もこの既定は変わりません）。Settings からいつでも手動で ON に切り替えられ、Reset は用紙枠を ON に戻します。
- 書き出しオプションの **出力形式** は、従来互換の **単一 HTML**（既定。画像をdata URLで埋め込む）と、**Web 公開用パッケージ（HTML と assets フォルダ）** から選べます。package は `index.html`、`assets/img/<sha256>.<ext>`、`assets/manifest.json` を作り、HTML内の画像は `./assets/img/...` を参照します。CSSとreader scriptは従来どおりHTMLへ内蔵します。
- package の保存先はフォルダ選択です。新規フォルダまたは完全に空の既存フォルダだけを受け付け、既存packageの更新・上書き・assets cleanupは行いません。書き込みは同じ親フォルダ内の一時stagingから完了後に公開するため、失敗時に既存のユーザーファイルを変更しません。
- Web Book内の画像は、画面閲覧時には現在の本文領域に縦横比を保って収まるよう縮小されます。小さい画像は拡大せず、画像だけの段落は可能な限り1ページ内に表示します。印刷は画面と同じ固定高さへ縮小せず、横幅超過と縦横比崩れを防ぐ範囲の簡易対応です。
- 画像の検証・容量判定はアプリ本体（main process）が行い、個別画像が 25 MiB を超える場合、画像の幅×高さが 1 億画素を超える場合、または単一 HTML の最終サイズが 100 MiB を超える場合は書き出しを中止します（package には単一 HTML の 100 MiB 上限は適用しません）。10 MiB 超の画像、単一 HTML が 50–100 MiB、package の画像合計が 200 MiB 超または 1,000 枚超のときは、保存ダイアログ／フォルダ選択の前に容量確認が表示されます。単一 HTML の強い警告では「単一 HTML として続行」「Web 公開用パッケージとして出力」「キャンセル」を選べます。ここでパッケージを選んでも、書き出しオプションに保存済みの出力形式は変わりません（その一回の書き出しだけがパッケージになります）。キャンセルするとファイルは作られません。
- 出力形式は `settings.json` の Web Book option slot に、現在文書とBook全体で別々に保存されます。frontmatter、Page Viewer設定、Readerの一時設定には保存されません。

| 操作 | 動作 |
| ---- | ---- |
| **File > 書き出し > Web Bookを作成...** | 現在の文書を `{stem}-web-book.html` として保存します。未保存タブでは `nyoze-web-book.html` を提案します。 |
| **File > 書き出し > Book 全体の Web Bookを作成...** | 現在の文書が属する Book の body 章を 1 つの `{Book名}-web-book.html` として保存します。 |

- 現在文書は通常表示（WYSIWYG）の現在内容を使います。`Source Mode`、`Paragraph Plain`、built-in の読み取り専用文書では実行できません。元の Markdown、frontmatter、Book manifest、付箋は変更しません。
- Book 全体は、他の Book export と同じく `.nyoze/books.json` v3 とディスク上の保存済み chapter を read-only で使います。Book の body 章以外ではメニューが無効です。未保存の編集は混ざらず、chapter が欠ける場合は部分出力しません。
- 保存前に HTML系の書き出しオプション確認 UI が開きます。改ページ、見出し前改ページ、文書/作品情報、章ファイル情報、目次、役割ラベル、書字方向を選べます。Web Book ではさらに「文書情報／作品情報の後ろで改ページ」（既定 OFF）と、「文書情報／作品情報を簡易表紙として表示」（既定 OFF）を選べます。簡易表紙を ON にすると、文書情報（Book では作品情報）が 1 ページ分の独立した表紙風ページになり（タイトルは本文見出しより大きな太字、著者・訳者はタイトルと別のまとまり）、直後の改ページが常に ON になります（OFF に戻すと直前の改ページ選択へ戻ります）。簡易表紙 ON のときだけ、レイアウト（「通常」= タイトル上付き・著者/訳者地付き、「中央」= ページ中央。縦書きの中央ではタイトルが上・著者/訳者が下になり左右中央に揃います。既定 通常）と、情報の書字方向（本文に合わせる / 縦書き / 横書き。簡易表紙の情報にだけ適用され、本文・目次は変わりません）を選べます。章ファイル情報は簡易表紙になりません。Book 全体の Web Book では、目次と右の Outline に各章タイトルが必ず出ます（本文に Markdown 見出しが無い章も含む）。章ファイル情報のタイトルは H1 相当の大きさで表示されます。これらの値は `document.webBook` / `book.webBook` にだけ保存されます。書き出し開始時点の Display Settings（見出し書体の意味論・配置・後余白・区切り線、および縦書き時の自動 TCY）と文書配色は HTML に埋め込まれます。カスタム書体名そのものは出さず、明朝系／ゴシック系／本文と同じ、のいずれかになります。自動 TCY は Editor / Page Viewer と同じ検出規則で表示用に分割され、縦書き表示（画面・印刷）のときだけ縦中横として見え、横書きでは通常文字のままです。Web Book の書き出し option や Reader の一時設定には出ません。Web Book 固有のテーマ・文字サイズ・書体・見出し書体・余白・header/footer・ページ遷移は、生成後に HTML 内だけで切り替える一時設定であり、書き出し option や Nyoze の設定には保存しません。
- options の直近確定値は `document.webBook` / `book.webBook` slot として `settings.json` に保存・復元します。frontmatter や `.nyoze/books.json` には保存しません。
- 出力開始時点の文書配色を安全な Author / Original palette snapshot として埋め込みます。配色情報が不正な場合は安全のため作成を中止します。
- Web Book は Chrome / Edge での画面閲覧、簡易印刷、PDF保存を正式確認対象とします。印刷所入稿品質の PDF、固定ページ組版、EPUB は対象外です。Firefox / Safari の印刷は best effort です。

### Book 全体の書き出し

通常の Markdown 保存とは別経路で、**現在開いているファイルが属する Book** の body 章（`.nyoze/books.json` **v3** の `books[].items[]` で `role: body` のファイル）を章順に結合し、1 つの外部ツール向けファイルへ書き出せます。

| 操作 | 動作 |
| ---- | ---- |
| **File > 書き出し > Book 全体を LeME 互換 Markdown で書き出し** | LeME 互換 Markdown（`.md` + HTML）へ書き出す |
| **File > 書き出し > Book 全体を でんでんコンバーター向け Markdown で書き出し** | でんでんコンバーター向け Markdown（`.md`）へ書き出す |
| **File > 書き出し > Book 全体を青空文庫風テキストで書き出し** | 青空文庫風 UTF-8 `.txt` へ書き出す |
| **File > 書き出し > Book 全体の Web Bookを作成** | 単一の reader 付き `.html` へ書き出す（Chrome / Edge での画面閲覧・簡易印刷/PDF保存向け） |

#### メニューの有効 / 無効（native File menu）

上記 4 項目は、**現在アクティブなファイルが Book の body 章**のときだけ File メニュー上で有効になります。次の場合は無効のままです。

- 資料（Materials）・未登録ファイル・作品外の単独ファイル
- built-in の読み取り専用 internal doc（ショートカット一覧など）
- books.json が invalid / read-error / diagnostics 付きで対象 Book を安全に解決できない場合

**未保存（dirty）**、`Source Mode`、`Paragraph Plain` 中でもメニュー項目自体は有効のままです（active document 単体の書き出しとは異なります）。ただし書き出し内容は後述のとおりディスク上の保存済みファイルだけです。

#### 書き出し元と未保存の扱い

- Book 全体 export は **ディスク上の保存済み chapter Markdown を read-only に読みます**。エディタ上の未保存変更（PM Doc）は含まれません。
- 未保存の変更があるときは、実行前に inline hint で「未保存の編集は反映されません」と案内します。実行はブロックしません。
- 成功時の通知でも「ディスク上の保存済みファイルから」と明記します。
- `Source Mode` / `Paragraph Plain` 中でも実行できます（disk read-only のため）。

#### Project metadata は変更しない

書き出しは **別ファイルへの export** です。次は変更しません。

- 開いているタブの保存先パス、File Explorer の表示
- Markdown 本文（各 chapter ファイル）
- frontmatter
- `.nyoze/books.json`、`.nyoze/notes.json`
- 付箋アンカー（`<!-- nyoze-note:... -->`）と付箋本文

#### 対象 Book の決め方

- 対象 Book は **現在アクティブなファイル**から main 側で解決します。renderer から project root は渡しません。
- 判定ではディスク上の `.nyoze/books.json` を read-only で参照し、作品 metadata を変更しません。

#### 章の欠落・読み取り失敗

- 1 章でも Markdown を読み取れない（ファイル missing、読み取りエラー、registry 上の path 解決失敗など）場合、**部分 export はしません**。エラー通知を出し、出力ファイルは作りません。

#### 書き出しオプション（確認 UI）

4 つの Book export いずれかを選び、対象外でないことを確認したあと、保存ダイアログの**前**に次の option 確認 UI が開きます。キャンセルした場合は書き出しを行いません（通知も出しません）。この options 確認 UI は、青空文庫風テキスト / LeME 互換 Markdown / でんでん向け Markdown / Web Book の active document 単体の書き出しとも共通です（各書き出し節を参照）。

| option | 概要 | 既定 |
| ------ | ---- | ---- |
| 改ページを反映する | 明示 `:::page-break` と見出し前自動改ページ設定を書き出しへ反映。オフにするとこれらの改ページは出力されない（`:::blank-page` はこの設定に関係なく常に出力される） | ON |
| 見出しの前で自動改ページ | 各見出し直前に自動改ページ。「改ページを反映する」がオフのときは無効 | OFF |
| 対象見出しレベル | 「見出しの前で自動改ページ」の対象を `H1のみ` 〜 `H1〜H6` から選択。「見出しの前で自動改ページ」がオフのときは無効 | H1のみ |
| 自動 TCY を反映する | **LeME / でんでん向けのときだけ表示**。短い英数字は両形式とも `^...^`。`!!` / `!?` / `??` は LeME では `<span class="tcy">...</span>`、でんでんでは `^...^`。Display Settings とは独立 | OFF |
| 数字だけを対象にする / 最小桁数 / 最大桁数 | **LeME / でんでん向けのときだけ表示**。「自動 TCY を反映する」がオフのときは無効 | OFF / 2 / 4 |
| 章の境界に改ページを入れる | Book の章ファイルが切り替わる箇所に改ページを挿入（**Book 全体の書き出し専用**。active document 単体の書き出しでは表示されません） | ON |
| 作品情報を表示 | **Book 全体の書き出しでは、LeME 互換 Markdown / でんでん向け Markdown / 青空文庫風テキスト / Web Book の 4 形式すべてで表示されます**（単独文書の書き出しには出ません。単独文書の書き出しには別途「文書情報を表示」という frontmatter 由来の項目があります）。`.nyoze/books.json` v3 の Book title / 著者（v3 の Book authors を「、」で連結）を本文冒頭に表示します（訳者は v3 に Book 単位の項目が無いため表示されません） | OFF |
| 章ファイル情報を表示 | **Book 全体の書き出しで、4 形式すべてに表示されます**（単独文書の書き出しには出ません）。各章ファイルの先頭に、その章の title / 著者 / 訳者（`.nyoze/books.json` の章メタデータ。章ファイルの frontmatter ではありません）を表示 | OFF |
| 役割ラベルを表示 | Book 全体の書き出しでは「作品情報を表示」または「章ファイル情報を表示」のどちらかがオンのときだけ有効（4 形式共通）。単独文書の書き出しでは「文書情報を表示」がオンのときだけ有効。著者・訳者行に「著　」「訳　」ラベルを付けるか | ON |
| 目次を表示 | **Web Book のときだけ表示**（単独文書・Book 全体の両方）。単独文書では本文中の見出し（H1〜H6）から目次を生成する。Book 全体では各章タイトルを章先頭項目として必ず出し、章内の Markdown 見出しをその下に続ける（見出しのない章も目次に出る） | OFF |

「作品情報を表示」「章ファイル情報を表示」で出る著者・訳者の行は、4 形式すべてで地付き（横書きでは右寄せ）で表示されます。また、これらのブロックの後ろには本文との間に約3行分の余白が入ります（title 行自体は右寄せ・余白の対象外です）。

option は **書き出しオプション確認で確定した内容**を、Book 全体の書き出し形式（LeME / でんでん / 青空文庫風 / Web Book）ごとに `settings.json` の `externalExportOptionsDefaults` へ保存し、次回以降の同形式の Book 書き出し時に初期値として復元します（Cancel では保存しません）。

「対象見出しレベル」は章境界の改ページ・明示 `:::page-break` には影響しません。それらは見出しレベルに関係なく維持され、「見出しの前で自動改ページ」と重複挿入されることもありません。

各形式への具体的な変換ルール（ルビ、TCY、配置、字下げなど）は、上記の active document 向け各節と同系統です。完全互換や完全準拠を保証するものではありません。

#### まだできないこと（Book export）

- ツールバー / 右クリックメニューからの Book 全体書き出し
- 書き出しオプションの project / Book 単位プリセット化（現状は `settings.json` の scope × format 別の直近確定値として保存。frontmatter / `.nyoze/books.json` には保存しない）
- 見出し配置（headingAlignment）の UI と実変換
- LeME／でんでん向け companion CSS / 設定ファイル、本文／見出し font の出力
- `.nyoze/books.json` を作品タブで更新したあと、active file が変わらない場合のメニュー有効状態の即時再判定（タブ切替やファイル再読込で更新されます）
- 変換 warning の詳細一覧 UI、書き出し前の強い preflight 確認
- Nyoze 本体による EPUB / PDF の直接生成（LeME / でんでん等の外部ツールでの変換は利用者側の作業です）

### `.txt` ファイルについて

Nyoze は `.md` だけでなく、`.txt` の文書も開いて編集できます。

- `ファイルを開く` では `.md` / `.markdown` / `.txt` を開けます
- すでに `.txt` で書いている小説やメモを、そのまま読み込んで使えます
- `.txt` で開いた文書は、`.txt` のまま保存できます

`md` と `txt` は、どちらも中身はテキストです。Nyoze では、そのテキストを Markdown として解釈して表示します。

ただし、古いテキストエディタや文芸向けエディタで作られた `.txt` は、Shift-JIS / CP932 のことがあります。Nyoze beta で実用上対応している文字コードは UTF-8 のみです。Shift-JIS / CP932 の `.txt` はそのままでは開けないため、先に UTF-8 へ変換してから使ってください。

そのため、Nyoze の中では見出し、太字、リストなどが見やすく表示されますが、普通のテキストエディタで開くと Markdown 記号はそのまま見えます。

例:

```text
# 見出し

**強調したい文**
```

これは Nyoze では装飾付きで見えますが、一般的なテキストエディタでは記号込みの文字として表示されます。

### 改行コードと文字コード

- 読み込んだ Markdown ファイルは、可能な範囲で元の改行コード（LF / CRLF）を維持して保存します。
- 新規作成した文書の既定の改行コードは LF です。
- beta 版では文字コードの完全な自動判定や、完全な保存往復には対応していません。実用上対応している文字コードは UTF-8 のみです。
- UTF-8 として読み込めないファイルは、文字化け状態で上書きしないよう通常編集対象として開きません。Shift-JIS / CP932 などの文書は、事前に UTF-8 へ変換してください。
- 改行コードが混在している文書は、保存時に先頭付近で検出した改行コードへ揃う場合があります。

### Markdown 保存時の正規化

Nyoze beta は、開いてすぐ保存しても Markdown の表記がまったく変わらないことまでは保証しません。表示上の意味を保ちながら、元の Markdown 表記が変わる場合があります。

beta で完全保持しない代表例:

- GFM table
- reference-style link / collapsed reference link / link definition
- footnote
- definition list
- 複雑な list / blockquote の空行、lazy continuation、tight / loose 表現
- code fence の文字種、長さ、末尾空行などの表記差分
- softbreak / hardbreak / `<br>` / two-space hardbreak の表記差分
- mixed EOL、BOM、複数 encoding

日本語の引用符つき強調については、`**「文学的な気分」**や**「情緒」**` のような単一装飾の隣接ペアは beta.1 時点で reopen / round-trip が安定するよう改善されています。

ただし、`***「文学的な気分」***や***「情緒」***` や `**~~A~~**や**~~B~~**` のように複合装飾が隣接するケースは、beta ではまだ delimiter の移動や正規化崩れが起きることがあります。こうした原稿は、保存前にバックアップを取るか、beta では読み取り確認に留めるのが安全です。

これらを含む文書では、保存前に別途バックアップを取るか、Nyoze beta では読み取り確認に留めてください。未対応構文をそのまま保持する仕組み、Source Mode の raw save、mixed EOL / BOM / 複数 encoding の完全な保存往復は post-beta 対象です。

### 保存前バックアップ

- `Save` の上書き保存時、および `Save As` で既存ファイルを上書きする場合、保存直前にディスク上の旧内容を自動バックアップします。
- 保存先に既存ファイルがない新規保存ではバックアップは作成されません。
- 保存先: `<app.getPath('userData')>/backups/<sha256(absPath)>/YYYYMMDD-HHMMSS-SSS<ext>`
- 同一タイムスタンプが衝突した場合はランダムサフィックス付きファイル名で保存します。
- 世代管理は元ファイルごとに最新20件を保持し、超過分は古いものから自動削除します。
- バックアップ作成に失敗しても本保存は継続されます（警告ログのみ）。
- バックアップフォルダはメニューの `File → バックアップフォルダを開く` から開けます。
- 現在開いているファイルのバックアップだけを確認したい場合は `File → このファイルのバックアップを開く` を使います。バックアップがまだない場合はその旨のダイアログが表示されます。未保存ファイルでは項目が無効になります。

未保存の変更がある状態で次の操作を行うと、確認ダイアログが表示されます。

- 未保存タブを閉じる（タブの `×` ボタン）
- アプリ終了 / ウィンドウを閉じる（いずれかのタブに未保存変更がある場合）

ファイル切り替え時（新規作成 / 読み込み）のダイアログでは以下を選択できます。

- キャンセル
- 保存して続行
- 破棄して続行

文書の新規作成/読み込み（`New Document`、`Shift + ファイルを開く`、`ファイルを開く`、左ペインのファイル切り替え）実行時は、切替先文書の `Document Type` / frontmatter override / タブ既定に応じた内部改行解釈を準備してから読み込みます。前のタブの解釈は持ち越しません。

終了時ダイアログでは以下を選択できます。

- キャンセル
- 保存して終了
- 破棄して終了

### 左ペイン（ファイルエクスプローラー）

| 操作                                           | 動作                                                                           |
| ---------------------------------------------- | ------------------------------------------------------------------------------ |
| フォルダ行クリック                             | 展開 / 折りたたみ（ツリー表示）                                                |
| 文書ファイル行クリック（`.md` / `.txt`）       | ファイルをアクティブタブに開く                                                 |
| ヘッダー `New Document`                        | 選択中フォルダ、または選択中ファイルの親フォルダに新規 Markdown ファイルを作成 |
| ヘッダー `New Folder`                          | 選択中フォルダ、または選択中ファイルの親フォルダに新規フォルダを作成           |
| 右クリックメニュー `Cut`                       | 選択中ファイルを移動対象として保持                                             |
| 右クリックメニュー `Copy`                      | 選択中ファイルを複製対象として保持                                             |
| 右クリックメニュー `Paste`                     | 選択中フォルダ（または選択中ファイルの親）へ貼り付け                           |
| 右クリックメニュー `Open in New Tab`           | ファイルを新しいタブで開く                                                     |
| 右クリックメニュー `Rename`                    | ファイル / フォルダ名を変更する                                                |
| 右クリックメニュー `Delete`                    | ファイル / フォルダを OS のゴミ箱へ移動する                                    |
| 右クリックメニュー `Reveal in Finder/Explorer` | OS のファイルマネージャで位置を表示する                                        |

- File Explorer は Finder / Explorer の代替ではありません。作品をまたぐ登録情報の移管、フォルダ配下一括追従、複数ファイル選択、ドラッグ＆ドロップ移動は対象外です。複雑なファイル整理は `Reveal in Finder/Explorer` から OS 側で行ってください。
- フォルダを書庫として使う場合は、File メニューの「書庫を管理」から登録します（ツールバーの「ファイルを開く」は単独ファイル open 専用です）。
- 左ペイン上部の **書庫** / **作品** タブはアイコンのみで表示され、ホバーまたはキーボードフォーカスでラベルが表示されます。**作品** タブ内で作品を開いているときは、breadcrumb の **一覧** で作品一覧へ戻れます。
- Explorer で開いているファイルは薄い背景で表示され、アクティブタブのファイルはより強い背景とアクセントで表示されます。
- 左ペイン下部の文書情報を展開すると、タイトル、著者、訳者、文書種別、書字方向、書庫、作品、役割、作成 / 更新日時、改行種別、パスを確認できます。作品内の登録済みファイルでは、タイトル・著者・訳者は `.nyoze/books.json` v3 の表示 metadata から出ます（frontmatter へ fallback しません）。役割（本文 / 資料種別）も books.json の登録情報から表示され、frontmatter `book` / `order` / `role` は使いません。
- 新規ファイル作成、名前変更、貼り付け、削除の直後は Explorer 表示が自動更新されます。
- タブやツールバーから作成した未保存文書を `Save As` で保存した場合も、保存先が現在の Explorer root 配下であれば Explorer に反映されます。
- Finder / Explorer など外部でファイルを削除・改名した場合も、アプリへフォーカスを戻したタイミングで Explorer 一覧を再読込します。
- `Copy` したファイルはツリー上で薄暗くしません（`Cut` のときだけ、移動元として薄暗く表示します）。貼り付けで同名競合ダイアログをキャンセルした場合はクリップボードがクリアされ、Cut 時の薄暗さも解除されます。

左ペイン最下部には、アクティブ文書の **文書情報** パネルが表示されます。
通常は「文字数」のみ1行表示され、右端のトグルで詳細を開閉できます（初期状態は折りたたみ）。

- 文字数（本文）
- ファイル作成日時
- 最終更新日時
- ファイルパス（省略表示、ホバーでフルパス確認）

未保存（`untitled`）文書では、日時は `—`、パスは `未保存` と表示されます。

同名ファイルが存在する場合は、競合ダイアログが表示されます。

- **両方とも残す**: `元の名前 2.拡張子` のように自動で別名を付けます（連番で空きが見つかるまで試行します）。**Copy** のときはその別名でコピーが作られます。**Cut（移動）** のときは、移動先の既存ファイルはそのままにし、移動中のファイルだけを別名で移動先に置きます（元のフォルダからはそのファイルがなくなります）。
- **キャンセル**: 操作を中止します。
- **置き換える**: 先にあるファイルを上書きします。

---

## beta 版の既知制限

- 正式なファイル読み込み導線はツールバーの「ファイルを開く」です。フォルダを書庫にする導線は File メニューの「書庫を管理」です。drag and drop、`Open With`、`.md` の OS 全体関連付けは未対応です。
- Linux 向け公式パッケージは現時点の beta では提供していません。公開ソースからの `npm install` / `npm run dev` / `npm run build` は試せますが、実機検証は未実施です。
- frontmatter の一般編集 UI はありません。GUI で編集できるのは Document Metadata の `Document Type` / `Paragraph Spacing > Preserve empty paragraphs`（Article のときだけ）/ `title` / `author` / `translator` だけです。作品内の表示 metadata は 作品タブ（books.json v3）が正本で、Document Metadata の frontmatter 編集とは同期しません。
- `Source Mode` は beta では raw save 専用導線ではありません。Apply / Save 時に Nyoze の Markdown 表現へ正規化される場合があります。
- 開いてすぐ保存しても Markdown の表記がまったく変わらないことまでは保証しません。GFM table、reference-style link、footnote、definition list、複雑な list / blockquote / code fence 表記などは保存時に正規化される場合があります。詳しくは [Markdown 保存時の正規化](#markdown-保存時の正規化) を参照してください。
- 日本語引用符つき強調のうち、`**A**や**B**` / `*A*や*B*` / `~~A~~や~~B~~` のような単一装飾の隣接は改善済みですが、`***A***や***B***` や `**~~A~~**や**~~B~~**` のような複合装飾が隣接する場合は、beta では reopen / 保存後に表記が崩れることがあります。
- WYSIWYG の code block シンタックスハイライトは表示のみです。言語未指定・未対応言語では自動判定せずプレーン表示になります。
- 通常編集上の文書内リンクは `Cmd/Ctrl + Click` で外部ブラウザに開けます。通常クリックでは開きません。開けるのは `https://` の認証情報なし絶対 URL のみです。
- ルビや明示 TCY（縦中横）の直後で日本語 IME 入力を始めたとき、環境やタイミングによっては、まれに 1 タイプ目の直後に 2 タイプ目で入力が詰まることがあります。その場合は `Escape` を押すと未確定入力を破棄して通常の編集状態へ復帰できます。
- 縦書きのルビ直後約物や、inline 装飾を含む通常段落の末尾閉じ括弧については、Chromium の行分割癖を避けるため表示専用補正を入れています。保存内容には影響しません。見出し / list / blockquote など通常段落以外の複雑なケースは、今後の追加確認対象です。
- Windows の一部 AMD GPU + Chromium 系環境では、本文上や `Source Mode` で I-beam マウスポインターが白く見えにくくなることがあります。その場合は `View Settings > 文書テーマ > エディタで矢印ポインターを使う` を有効にすると、本文上だけ矢印ポインターへ切り替えて回避できます。
- `Paragraph Plain` は通常表示とできるだけ近い見た目になるよう調整していますが、ウィンドウ幅やペイン幅の境界によっては、まれに overlay textarea 側だけ 1 文字ぶん多く、または少なく折り返されて見えることがあります。保存内容や block 構造には影響しません。
- 10万文字前後から、環境によっては入力や描画が重くなる場合があります。特に縦書き・ルビ表示・検索 ON・日本語 IME 入力の組み合わせでは重くなりやすく、ルビを多用した文書ではそれより少ない文量でも影響が出ることがあります。
- 重く感じたときは、まずルビ表示をオフにする、`Paragraph Plain` を使って編集する、それでも重い場合は章などの区切りのよい単位でファイルを分ける、といった運用をおすすめします。
- beta で実用上対応している文字コードは UTF-8 のみです。UTF-8 として読み込めないファイルは通常編集対象として開きません。Shift-JIS / CP932 などの文書は、事前に UTF-8 へ変換してください。混在 EOL、BOM、複数 encoding の完全な保存往復は post-beta 対象です。
